# Re-audit of every closure against the corrected target (increment 199)

*Correction #30 (increment 198) found that this program had been
carrying a misstated target: the consumable was printed as
Σ_{k∼K}|D(k)|² ≪ (log N)^{−A} Σ M_k — the **square-root** scale —
where the chain actually requires*

> **Σ_{k∼K}|D(k)|² ≪ (log N)^{−2A−2} Σ_{k∼K} M_k²**,

*i.e. a fixed log-power saving over the **trivial** bound. The two
differ by a factor M ≍ N/K, which for K ≤ N^{1/3} is ≥ N^{2/3}.*

**Therefore every death sentence that was passed on a magnitude,
budget, or scale-mismatch argument was measured against the wrong
yardstick and must be re-opened.** This document does that, closure by
closure. It is written to find our own errors, not to defend the
record.

## The decisive quantity

Under the corrected target the proof has a **slack of N/K ≥ N^{2/3}**:
a technique may be lossy by up to that factor and still deliver. Under
the printed target the required bound sat *below* what nature supplies,
so the slack was zero and **any** loss was fatal. That single change
is what re-opens things.

Note the slack is specific to the E1 route. For the scalar C(N)
itself, trivial is ψ(N) ∼ N and the target is N(log N)^{−A}, so the
slack there is only a log power — Proposition E (circle-method zero
margin) is unaffected.

## Verdict table

| Closure | Basis of death | Re-audit |
|---|---|---|
| **C-III #1** — §5 assembly, "scale mismatch x^{2/3}" | **magnitude** | **VOID** — see below |
| C-III #2 — classification hides a type-II region | structural | stands |
| C-III #3 — the dual object is the output of no legitimate transform | structural | stands |
| **C-III #4** — pointwise budget deficit x^{1/3} | **magnitude + structural** | **first half in question**; second half stands |
| RV #1 — gate arithmetic, Q-slot premise violated | structural (premise) | stands |
| RV #2 — role exchange, no pair congruence | structural (hypothesis) | stands |
| RV #3 — SEAM over-normalized by √P | **normalization** | stands, but see "same error class" |
| **RV #4** — h-range collapse, "excess K^{2/3}" | **magnitude** | **in question** |
| **RV #5** — second Vaughan layer multiplies the conductor | **magnitude + structural** | **in question** (first half) |
| **RV #6** — Theorem E1 (L²) does not match the pipeline (signed L¹) | **norm/currency** | **in question** — this is the same error class as #30 itself |
| Adjudication routes 1, 3 | structural (lemma hypotheses) | stand |
| Adjudication route 2 | structural ×2 + "triple-log saving below spec" | stands (triple-log is still not a fixed log power) |
| Adjudication route 4 | structural + "Perron costs N^{1−o(1)}" | stands, but the margin is now N^{1/3}, not "hopeless" |
| Adjudication route 5 | structural (exceptional set) | stands |
| Forge K1–K4, R1–R4 | existence-of-structure kill-tests | stand — each measured *no* signal, not *insufficient* signal |
| Forge R5 (circle method) | magnitude, but on C(N) | stands — no power slack on that route |
| Construction C1, C2, C4 | existence-of-structure | stand |

## C-III #1 is void

The verdict reads, verbatim:

> "E1 is an L²(k) statement at GAUSSIAN (square-root) scale: it demands
> |D(k)|² ≈ M_k. Every leaf of the tree outputs LINEAR-scale pointwise
> log-savings (|D(k)| ≪ (x/k)(log)^{−C}). Squaring gives
> Σ|D|² ≪ (N²/K)(log)^{−2C} — off target by N/K ≥ x^{2/3} at every
> J, K, A."

The premise in the first sentence is exactly the misstatement of
correction #30. The corrected target is

> Σ|D|² ≪ (log N)^{−2A−2}·N²/K,

and the tree's own output, as the verdict itself computes it, is

> Σ|D|² ≪ (N²/K)(log)^{−2C}.

**These are the same statement**, and the leaves meet the target
whenever C ≥ A+1. The "off target by N/K ≥ x^{2/3}" was the distance
to the square-root scale, which is not the target and which nothing in
the chain requires. **Refutation #1 does not hold.**

Consequence for the headline: C3_REVIEW claims "four independent kill
coordinates — two of which stand even if Lemma S is granted for free",
and those two were #1 and #4. #1 is void and #4's arithmetic half is in
question. **The claim that the tree dies even with a free Lemma S is
withdrawn.**

## What this does and does not re-open

**Does not**: C-III is still refuted *as drafted*. Coordinate #3 is
fatal to the specific construction — the draft's hybrid object
(Kloosterman factors, √-phase, 1/c weights and a c-range at once) is
the output of neither legitimate transform — and coordinate #2 shows
the classification leaves an unhandled type-II region. Those are
independent of any target.

**Does**: C-III is no longer *impossible on budget grounds*. It moves
from "dead even with a free Lemma S" back to "an open route requiring
a correct construction", and its remaining obstruction (C-III #4,
second half) is now a *named* target rather than a scale gap: all
saving must come from u′-family sign cancellation, which expands into
shift-averaged binary μμ correlations needed **at log-power strength**
— MRT/Pilatte territory, where the best known savings are
(log)^{1−c} for a small fixed c, i.e. short of a fixed power but in an
actively moving area.

## The same error class, twice

RV #3 killed the SEAM formalization for being "over-normalized by √P
— it demands more than the chain needs". That is the identical species
of mistake as #30: **this program has twice written its own target at
the wrong power of the natural scale.** RV #3's verdict stands (the
SEAM statement as written was indeed too strong), but the corollary is
that **SEAM should be re-derived against the corrected target before
it is called false** — an over-strong conjecture that is false is not
evidence against the correctly-normalized one.

Named as a standing hazard: *scale-normalization drift* — writing a
target at the square-root scale when the chain consumes it at the
trivial scale, or vice versa. Both known instances were caught only by
comparing a stated target against our own measurements of the same
quantity. That comparison is now mandatory before any target is used
to adjudicate anything.

## Round 2 (increment 200): the four open items, settled

### RV #6 — which norm the chain consumes: **downgraded, not fatal**

RV #6 objected that "the tex's Theorem E1 (L²) does not match the
pipeline (signed L¹)". Settled from scratch, since it is a
norm/currency call and that is the very class of error #30 was.

The chain's actual consumable is the **signed** sum
T_II = Σ_{k∼K} b_k D(k) ≪ N(log N)^{−A}, with b_k the specific
arithmetic weight the Vaughan decomposition produces (1, log k, …),
not an arbitrary bounded sequence. The L² statement enters **only**
through Cauchy–Schwarz, |T_II| ≤ ‖b‖₂(Σ|D|²)^{1/2} — a proof strategy
we chose, not a requirement. So L² is *sufficient* (the implication
runs the safe way) and *strictly stronger than needed*.

Measured price of that step (`code/norm_audit.py`, N = 10⁸):

| K | 10³ | 3·10³ | 6·10³ |
|---|---|---|---|
| (Σ\|D\|²)^{1/2} | 2661 | 1423 | 774 |
| \|Σ_k D(k)\| | 795 | 550 | 14 |
| ‖1‖₂·(Σ\|D\|²)^{1/2} | 46095 | 24654 | 13403 |
| **Cauchy–Schwarz loss** | **58×** | **45×** | 957× |

The loss is ≍ √K, as it must be for a sign-random field (√1000 = 32,
√6000 = 77; the 957 is one band where the signed sum happened to land
near zero). Both norms are cleared by nature with room — the signed
sums sit 5–7 orders below N — so the √K loss is affordable.

**Verdict: the mismatch is real as an observation and not fatal as an
objection.** Downgraded from "refuted (major)". The strategic
corollary matters more than the verdict: **the chain needs only the
signed L¹ bound, and Cauchy–Schwarz discards exactly the sign
structure of b_k** — the structure whose power R4 already exhibited
(for b_k ≡ 1 the *unrestricted* sum is exactly μ(N−1)). The program
has been aiming at something strictly harder than its own target.

### C-III #4 arithmetic half — **void**

The verdict's own words are "**No-margin** arithmetic: pointwise
consumption loses by N_n^{1/2} ≥ x^{1/3−O(1/J)}". The no-margin
premise is the misstated target. Against the corrected slack N/K ≥
N^{2/3}, a loss of x^{1/3} is affordable, with N^{1/3} to spare.

The structural half also softens. "The object is typically exactly
√-sized, so absolute-value summation reaches at best trivial scale" is
no longer a death sentence: **the corrected target is exactly one
log-power below trivial**, so reaching trivial scale leaves a gap in
the currency rather than a gap in the exponent. What remains is the
verdict's own last clause — all saving must come from u′-family sign
cancellation, which expands into **shift-averaged binary μμ
correlations at log-power strength**.

**Verdict: arithmetic half void; structural half downgraded from
"fatal" to "reduces C-III to a named open problem".**

### RV #4 and #5 — **moot**

Their budgets (the "excess K^{2/3}", the conductor inflation) were
computed against the printed target and are probably void by the same
arithmetic — at K = N^{1/3}, K^{2/3} = N^{2/9}, far inside a slack of
N^{2/3}. But re-deriving them would be archaeology without payoff:
they belong to the proof program that RV **#1 and #2 kill
structurally** (a violated Q-slot premise; an absent pair congruence),
and those two are untouched by any target. Re-opening #4 and #5
revives nothing.

**Verdict: budget arithmetic likely void, immaterial.**

## Where this leaves C-III

All four kill coordinates re-audited: **#1 void, #4 void/downgraded,
#2 and #3 fatal to the draft as written but not to the route.** The
headline "the tree dies even with a free Lemma S" is withdrawn in
full.

What C-III now needs, stated exactly:

1. a **legitimate transform** in place of the draft's hybrid object
   (coordinate #3 stands: Kloosterman factors, √-phase, 1/c weights
   and a c-range are not simultaneously the output of truncated
   Voronoi or of the delta method);
2. a **complete classification** covering the type-II region the draft
   's bookkeeping hid (coordinate #2);
3. **quantitative averaged Chowla**: shift-averaged binary μμ
   correlations with a *fixed log-power* saving. Best known is
   (log)^{1−c} for a small fixed c.

Item 3 is worth stating plainly because it changes the character of
the obstruction. The adjudication's central finding was that the
**dilate** average admits no diagonalizing character family, whereas
the **shift** average is precisely MRT's home ground (translation,
Fejér kernel). If a legitimate transform converting the one into the
other exists, the remaining gap is *quantitative strength inside an
active area*, not the absence of any coupling surface. That is a
different kind of open problem from the one this program had recorded.

**None of this is progress toward Goldbach.** It is the withdrawal of
over-claimed refutations. The route is open again and needs exactly
the thing nobody can currently do.

## Round 3 (increment 201): item ① probed directly

C-III's remaining need ① is "a legitimate transform". Stated cleanly,
the geometry is this. In the L² off-diagonal put a = N−mk, b = N−m′k;
then **m′a − mb = (m′−m)N**, so as k runs the pair traverses lattice
points on a fixed line of rational slope m/m′. A shift correlation
Σ_a μ(a)μ(a+h) is the same object on a line of **slope 1**. So

> dilate problem = μμ correlations along lines of arbitrary rational
> slope; shift problem = the slope-1 case.

Item ① asks for a transform carrying the first to the second. Two
things are now measured (`code/c3_transform_probe.py`).

**(i) The obvious shift reading is an exact relabelling, not a
transform.** Re-index by d = m′−m and fix (d,k): the shift h = dk is
then *fixed* and a = N−mk runs over the progression a ≡ N (mod k), so
the inner factor genuinely is a fixed-shift correlation
Σ_{a≡N(k)} μ(a)μ(a−dk). But **the shift dk is a multiple of the
modulus k**, and rescaling the progression sends μ(a)μ(a−dk) straight
back to μ(N−mk)μ(N−(m−d)k). Verified numerically: the off-diagonal
computed directly and via the (d,k) reading agree **exactly** (184.0
vs 184.0, difference 0.000e+00). The shift reading exists and is
circular.

**(ii) The slope family is not tamer than the shift family.**
Pre-registered: LEAD iff mean |T(m,m′)|/√K ≤ 0.6, CLOSED if the two
families agree within 10%.

| K | slope mean | shift mean | ratio |
|---|---|---|---|
| 10³ | 0.4561 | 0.4826 | 0.945 |
| 3·10³ | 0.4714 | 0.4591 | 1.027 |

**CLOSED.** Both sit at half-normal on their own support (0.46/√0.37 ≈
0.76 against 0.798, the mask accounting of Conjecture L), and they
agree with each other. The two families are the same difficulty; the
difference between them is provability, not size.

**What ① therefore requires.** Not a re-indexing (circular) and not a
motivation from the slope family being tamer (it is not). It must be a
genuine *analytic* transform — a summation formula whose dual variable
supplies the shift average. The named candidates for that are exactly
the ones already measured shut: the inverse-domain spectrum (C2,
mask-exact under accurate nulls), manufactured modularity (C4, 0/6
levels), and the dispersion/delta route (REVIEW_VERDICT #1–#2,
structurally refuted).

## Round 4 (increment 204): ① closed for changes of variable

The centered coordinates settle half of ① by proof rather than by
measurement. Put A = N−a, B = N−b, so A = mk and B = m′k; then

> **m′A − mB = 0 exactly**

(verified over 200,000 triples, zero mismatches,
`code/c3_pencil_check.py`). So the dilate family is the **pencil of
lines through the origin** and the shift family B = A − h is a family
of **parallel lines of slope 1**. A pencil is characterised by its
vertex — here a finite point — and a parallel family is a pencil whose
vertex is at infinity. **Affine maps send finite points to finite
points**, so no affine map carries one family to the other; and since
μ lives on ℤ, the structure-preserving changes of variable available
are exactly the integral affine ones. No shear helps either: it
permutes slopes and fixes the origin.

**So ① is closed for pointwise changes of variable**, which explains
the measured circularity rather than merely recording it. It remains
open for summation formulas — where the obstruction is coordinate 3's,
in general form: Voronoi-type formulas compress only against a
**smooth** outer weight, and ours is μ.

**Status of ①**: closed in one of the two classes a transform can
belong to, unpopulated in the other. This is a classification of
transform types, not a theorem about all conceivable mathematics, and
is stated at that strength. **C-III therefore stands at: ① half
closed, ② unattempted, ③ a named external open problem.**

## Round 5 (increment 205): ② settled — the hidden leaf is the bulk

C-III's requirement ② was "a classification covering the type-II
region". Attempting it settles it, negatively for the draft and more
sharply than the review had stated.

The draft's CO bookkeeping needs the μ-side a to satisfy
a ≤ y^{O(1)} = M^{o(1)}. Measuring the absolute Heath–Brown weight
W(a) = Σ_j C(J,j)·A_j(a)·D_j(M/a) across a-sizes
(`code/c3_hb_mass.py`):

| J | a > M^{0.05} | a > M^{0.10} |
|---|---|---|
| 3 | 0.939 | 0.819 |
| 4 | 0.949 | 0.826 |
| 6 | 0.960 | 0.836 |
| 8 | 0.947 | 0.750 |

and raising M to 10⁶ moves these to 0.961/0.865 (J=4) and
0.969/0.849 (J=8). **So ~95% of the identity's weight — rising with
M, flat in J — lies outside the region the draft's classification
covers.** The excluded pieces carry a rough coefficient α(a), a
product of μ-blocks with no divisor structure, hence no Voronoi
entry: §3's spectral door serves only the remaining few percent.

**Verdict on ②**: the classification can be completed, and completing
it shows the tree's dominant leaf is the bilinear μμ core with no
spectral entry. This is scoped to the one-sided Heath–Brown opening as
drafted, over J = 3…8 and two M; a different opening could reallocate
mass, and nothing here rules that out.

**Can the draft be repaired by reparameterising?** No, and the reason
does not depend on parameters. The identity with cut z needs z^J ≥ x,
and its j-th term has μ-side a = a_1⋯a_j ≤ z^j, which at j = J is x —
for every admissible (z, J). So a ≤ z^{O(1)} is compatible with the
identity only if its high-j terms are dropped, which destroys it. And
those are the terms carrying the weight: measured mass by block count
is 0.824 at j = 3 (J=3), 0.967 at j ∈ {3,4} (J=4), 0.806 at
j ∈ {5,6} (J=6), 0.847 at j ∈ {6,7,8} (J=8).

**What a repair would require.** The excluded region is a bilinear
form Σ_a Σ_b α(a)β(b)μ(N−abk) with α rough and a long. Each piece is
individually trivial — for a near M the progression w ≡ N (mod ak)
holds O(1) terms — so all the content is cancellation *across* a,
which is a type-II estimate for μ(N−·): the wall. Cauchy–Schwarz in b,
the only available move, returns the dilate-pair correlation.

This is not special to the one-sided opening. Every identity
decomposing μ produces a type-II term, and an identity whose every
piece had either a long free variable or a divisor-structured
coefficient on the rough side would dispose of the parity obstruction.
**"Complete the draft" and "break the wall" are the same task.**

**A methodological correction, same species as #30.** The first pass
pre-registered a single threshold, a > M^{1/3}, and got a J-dependent
answer (0.326 → 0.032 as J ran 3 → 8) that pointed the opposite way.
That threshold does not match what the claim requires: "a ≤ y^{O(1)}"
means a = M^{o(1)}, i.e. small θ, not θ = 1/3. **Choosing a threshold
that does not match the claim being tested is the same error as
writing a target at the wrong scale** — and it was caught the same
way, by widening to the whole profile instead of trusting one number.

## Round 6 (increment 227): the hazard-4 sweep

Hazard 4 is new, so the same question #30 raised has to be asked again:
**does any standing closure rest on a null that was a size heuristic?**
Every null in `code/` was classified.

| null construction | scripts | exposed? |
|---|---|---|
| Monte-Carlo draws from the model, scored with the *same* estimator | `e1_constr_c1/c2/c2b/c4`, `e1_spectral_null`, `e1_wishart_null`, `e1_forge_kt3/kt4`, `e1_dilation` (permutation), `e4_wheelnull` | **no** — the null is generated, not guessed |
| exact sampling distribution of the statistic, or SE computed from the data as `std/√n` | `sweep_A` A6–A10, `sweep_B` B1–B5, `sweep_B2/B3`, `hyp_round2b`, `lab_gb_multiplicativity`, `e1_forge_kt2` | **no** |
| verdict is directional ("does the ratio approach 1"), with an a priori `(log N)^{−1}` band printed for scale only | `hl_S1_check`, `hl_assembly` | **no**, but the band is heuristic — the verdicts do not turn on it, and both landed at 0.982 and 1.002 |
| CLT exponent 0.5 as the null, with a hand-chosen band [0.45, 0.60] | `sweep_C` | **low** — the exponent is exact under the null; only the band width is judgement, and the reading was "every variant sits at square root" |
| **size estimate `√(M_p log N)` in place of the second moment** | `lab_prime_factor_split` | **YES** — recomputed |
| **Euler-product and `√(N/w) log N` size estimates** | `lab_grouping_asymmetry` | **YES** — corrections #33, #34, fixed in the same session |

**Result of the recomputation.** Session 6's null was replaced by the
exact second moment `V_p = Σ_{v<N,p|v} μ²(v)Λ(N−v)²`. The constant moved
from 0.83 to 0.87 and `S_abs/S_null` remains flat (0.883 → 0.871 across
a factor 8 in N); `S_abs/triv` is null-free and unchanged. The heuristic
sat 5–7% high — except in the top dyadic range, where it returned a
"typical size" of 1.0968 times the trivial bound, which is impossible;
the exact second moment returns 1.0000 there, correctly reporting that
one-term sums admit no cancellation at all. **The verdict stands: the
prime-factor split has positive margin.**

**Nothing re-opens.** And there is a structural reason it was unlikely
to: hazard 4 bites on *magnitude* nulls, whereas the closures that
survived rounds 1–5 are the ones whose criterion was "is there any
signal at all", which is threshold-free and therefore null-shape-free.
The two exposed scripts are both from this week's invention work, where
the questions are quantitative for the first time.

**Coverage, stated.** This swept nulls, not closures. A closure resting
on a *correct* null but a wrong model is untouched by it — and several
E1 verdicts use Conjecture L's own iid model as the null, which is
circular by construction and already recorded as such in `sweep_B`.

## The six hazards this campaign actually suffers from

Both are about **stating a criterion that does not match the thing
being tested**, and both are caught the same way — by widening from a
single number to the whole profile, or by computing the null first.

1. **Scale-normalisation drift.** Writing a target at the square-root
   scale when the chain consumes it at the trivial scale, or the
   reverse. Two instances: the E1 target (correction #30) and the SEAM
   formalisation (REVIEW_VERDICT #3). Rule: state which scale a target
   is normalised against, in the same sentence, and compare it against
   our own measurement of the same quantity before using it to
   adjudicate anything.

2. **Thresholds below the noise floor.** Choosing a pre-registered
   cutoff without computing what the statistic reads under no
   structure. Five instances in a single session: the a > M^{1/3} cut
   (increment 205), the spread-only test for a mask on C(N) (210), the
   |corr| ≥ 0.10 cross-N test and the top-decade test (212), and a
   verdict string that misreported its own rule (211). Rule:
   **compute the null value of the statistic first and put it in the
   pre-registration next to the threshold.**

3. **Identity checks with unenforced side conditions.** Verifying an
   arithmetic identity while imposing only some of its hypotheses, and
   reading the resulting failures as a defect. Three instances in one
   session: the ladder identity needing p² ∤ m (increment 218, ~10%
   false failures); the μ² insertion, where the paper inserts μ² into
   the product *before* the split and we applied it after (221) — this
   one would have become a **false claim of a second defect in a
   published paper**; and the μ–μ dilation needing p ∤ v *and*
   p ∤ (N′−v) (224, 5.2% false failures). Rule: **when checking an
   identity with side conditions, enumerate them all and enforce every
   one before reading any mismatch.**

4. **Nulls taken from size heuristics instead of from the data.** Rule
   2 says compute the null first; it does not by itself say *how*, and
   an a priori estimate of the null is still an assumption. Two
   instances at increment 226 (corrections #33, #34): an Euler-product
   reading gave `A(W) ~ c/log W` where the truth is PNT-strength, and
   `|E_w| ~ √(N/w) log N` overshot the measured residual by a factor 7
   because it is dominated by the w where the model does not apply.
   Rule: **where the statistic's own second moment is computable from
   the same loop, use it as the null.** Print the heuristic beside it
   if it is informative, never in place of it.

5. **Reading a declining trend as convergence to the null, when the
   statistic has no power to distinguish.** Four instances, all on the
   same object and all found only at increments 248–254: `sweep_B`'s B4
   sign balance (z = −1.55 on ~1500 values; **−94.3 and +61.6** on
   1.95·10⁶); B5's `corr(C,𝔖)` falling −0.194 → −0.138 → −0.070 across
   three windows and read as convergence (**z = −193.5** at full power,
   and **−137.4** after the very fix that was supposed to remove it);
   the "mean drift, already insignificant at N ≈ 9·10⁵"; and §10's
   `3 | N` split, where "𝔖-normalisation takes it below 3σ" was n ≈ 500
   and full power gives z = −70 after that same normalisation. Rule:
   **a trend toward the null is not evidence of the null unless the
   last point has the power to reject it.** Quote the power, not the
   trend.

6. **A step that silently does not run, while the record says it did.**
   The most dangerous of the six, because nothing looks wrong. Two
   instances: a pre-registered control prime dropped by an `n ≥ 12`
   guard that printed nothing (increment 243), and a document rewrite
   whose script raised `AssertionError` on a string that did not match
   while the surrounding shell went on to commit a message describing
   the rewrite as done (increment 254, corrected at 255). Rule:
   **chain the edit to the commit so a failure aborts both, and make
   every skipped branch print the fact that it skipped.** A control
   that does not announce its own absence is worse than no control.

   **It then recurred three times immediately after being named.**
   `sed -i 's/^\*Increment 255.../.../'` matches nothing when the file
   says 254, and `sed` exits 0 on no match, so increments 255–257 each
   reported a bump that did not happen and STATUS.md sat at 254 for
   three commits. The rule above is necessary and not sufficient: a
   chained command that *succeeds while doing nothing* defeats it.
   **Every edit must be followed by a read-back that asserts the new
   state**, which is what the fix at 258 does. Naming a hazard does not
   prevent it; only a check that fails loudly does.

   **A third form: a check that cannot fail.** Increment 272's
   identity test printed a "C(N) − Λ(N−1)" line built by rearranging
   the very quantity it was meant to confirm, so it agreed to all
   digits by algebra and verified nothing. It also compared
   `Σ_p log p D_p` (weight 1) against ω-classes built with a `1/log v`
   weight — two different objects, and the mismatch showed up as a
   1.5·10⁴ "failure" of a true identity. Both fixed; with the weights
   matched and a genuinely independent third computation the identity
   holds to 6·10⁻¹⁰. Rule: **a verification must be able to come out
   false.**

   **A fourth form: a shell heredoc collapsing `\\` to `\`.**
   At increment 280 a patch reported success and wrote `\text` into
   STATUS.md as a literal TAB and `\asymp` as a BEL, because the
   heredoc ate one level of escaping before Python saw it. The
   assertions passed — they tested for the *surrounding* text, not the
   escapes — and only reading the file back caught it. Repairing it
   exposed that this very paragraph had been spliced into the middle of
   a sentence by an earlier edit of the same kind, unnoticed since.
   Rule: **assert on the exact bytes that were the point of the edit**,
   and prefer a file-writing tool to a shell heredoc for anything
   containing backslashes.

   **A fifth form, and it broke the rule written one increment
   earlier: `open(path, "w")` truncates before it can fail.** Writing
   `𝔖` (U+1D516, outside the BMP) as a surrogate pair `𝔖`
   raises `UnicodeEncodeError` at the `.write()` — but the file has
   already been emptied by the `open`. This destroyed FINDINGS.md at
   increment 280 (recovered from context; it is deliberately outside
   git, so there was no other copy) and CLOSURE_REAUDIT.md at 281
   (recovered by `git checkout`). Rules: **write to a temporary path
   and rename**, or use the editing tool rather than a shell heredoc —
   which is exactly what the previous paragraph said to do, one
   increment before it was ignored. Naming a hazard does not prevent
   it, and neither does having just named it.

Every closure in this repository predating these rules was re-checked
against them; the ones that survived are the ones whose criteria were
"is there any signal at all", which is threshold-free.

## Supersession record (all of it, in one place)

This repository carries no correction banners elsewhere. Every
document states its current position directly; what was withdrawn to
get there is listed here.

| # | Withdrawn statement | Replaced by |
|---|---|---|
| 25 | "gates pass"; "the remainder is one 1/30-wide seam"; "the rest is transcription" | REVIEW_VERDICT.md items 1–3 |
| 26 | a z = 9 spectral excess (iid-entry Wigner null applied to a Gram matrix) | the correct Wishart null, dead centre |
| 27 | "C-III dies even with a free Lemma S" | C3_REVIEW.md as it now stands: draft refuted, route open |
| 28 | Theorem A's bound is N e^{−c√log N} | ≪_A N(log N)^{−A}; BV's internal Siegel–Walfisz range caps it |
| 29 | in Prop. D″, the top-r piece dominates by a power of log N | the pieces are the same order (r₂/r₁ = 0.771 → 0.810); the closure rests on nonnegativity |
| 30 | E1 target `Σ|D|² ≪ (log N)^{−A} Σ M_k` (square-root scale) | `Σ|D|² ≪ (log N)^{−2A−2} Σ M_k²` (trivial scale) — a factor N/K |
| 31 | "no route verdict changes" after #30 | this document: 2 void, 1 downgraded, 2 moot, 13 standing |
| 32 | ③ is "a named external open problem" with a qualitative gap; MRT's averaged Chowla is qualitative | an **exponent** gap: MRT gives ≈(log H)^{−1+o(1)}, Pilatte a fixed power, and the chain wants ≈(log N)^{−6} — provisional, since the 6 is read off Huang–Li (TRANSFORM_LAB §2) |
| 33 | null for the singular-series average: `A(W) ~ c/log W` from the Euler product 1 − f(q)/q ~ 1 − 1/q | `A(W)` tracks `Σ_{w≤W} μ(w)/w`, PNT-strength — measured ~10⁻³ against 1/log W ≈ 0.09. The Euler reading describes the smooth-truncated product, not the Möbius sum |
| 34 | null for the w-grouping residual: `Σ_w|E_w| ~ √2·N log N`, so the ratio to trivial should be ≈1 and flat | the data-driven second-moment null `Σ_w √(Σ_p t_p²)`; measured ratio 1.18–1.26, flat, and 0.26 of trivial and falling. The heuristic overshot by a factor 7 |
| 35 | the criterion "does `S_abs/N → 0`?" tested as `(log N)^{−1/2}` **against** `c + b/log N`, returning DEAD | the right comparison is free-exponent (→0) against `c + b/log N` (→c). Rejecting the *rate* −1/2 is not evidence for a positive *limit*. Corrected verdict at N ≤ 6.4·10⁶: **UNDECIDED** — the two fit equally well (resid sd 0.00081 vs 0.00083) and separate by 0.0035 at N ≈ 5·10⁸, below the noise. What *is* settled: the exponent is ≈ **0.773**, i.e. **faster** decay than the square-root-per-p prediction |
| 181 | *(the natural extension of the closure I had just celebrated, refuted in one run)* After #180 measured the mask's exponent per depth, the obvious next claim was that A2's closed form — the floor's size is the singular-series excess over same-cell pairs, `dm_c ≈ a·D_c` with `D_c = E_same,c[S₂] − E_all[S₂]` — should also govern the floor's **decay**, with the unknown `a` cancelling from the differences because it is common to all six depths | ❌ **Refuted, and structurally rather than numerically.** Computing `D_c` **exactly** (an FFT autocorrelation of the cell indicator against `S₂`, no sampling — the sampling in increment 312 is what A2 died of) gives predicted exponents of `−0.0052, −0.0070, −0.0017, −0.0005, −0.0002, −0.0003`, i.e. **all of them zero to within their own errors of `0.0001–0.0007`**, against measured exponents of `0.14` to `0.63`. The prediction is 30 to 100 times too small, and the residual is **99%** of the raw spread. ⚠️ **The reason is forced, not accidental**: `D_c` depends only on the distribution of `h` in the residues mod `3,5,7,11,13` that the cell fixes, which is the same at every scale — so `D_c` is **scale-invariant**, measured flat across a factor 160 in `N`. A2's closed form is therefore *incapable in principle* of producing any decay, let alone a depth-dependent one. ⚠️ **And P2's "pass" is nearly vacuous and is recorded as such**: the Spearman correlation landed on exactly the pre-registered bar (`+0.900`, one adjacent swap) between quantities differing in magnitude by two orders, so it is not evidence for the mechanism. **The conclusion is that the mask's decay comes from the normalisation, not from the arithmetic** — and since `a` is common to all depths, whatever produces *different* exponents per depth is something the closed form omits entirely |
| 180 | *(hazard 8 aimed at A3, and for once the existing claim survives and gets stronger)* "there is no single mask exponent", asserted at increment 305b from a fourfold spread in which **not one of the six exponents carried a standard error** | ✅ **the spread is real, and now it is a measurement.** Supplying the errors from the weighted fit's own covariance, with increment 305's exact per-cell errors as weights: `a = 0.1434 ± 0.0155` at depth 5, `0.2152 ± 0.0065`, `0.2713 ± 0.0040`, `0.3686 ± 0.0052`, `0.0437 ± 0.0556` at depth 1, `0.6289 ± 0.0121` at depth 0. A common exponent is rejected at **`χ²/dof = 251`** on 5 dof. The reconstruction reproduces 305b's six point estimates to four decimals, so it is the same fit. ✅ **And it sharpens A3 rather than only confirming it**: excluding depth 1, the exponent rises **monotonically as the cell gets shallower** — `0.143, 0.215, 0.271, 0.369, 0.629` — with the steps at 5 to 30 standard errors. **The mask decays faster where fewer small primes divide `N`.** ⚠️ Depth 1 (`3 \| N` alone) is the exception at `a/SE = 0.8`: its cell is large and its mask small, so the exponent there is **not measurable**, which is consistent with 305b's own "no decay established" at that depth |
| 179 | *(a circular criterion caught in my own draft before it ran)* increment 333's first version, which back-solved the coefficient `a` of `ρ(h) = a𝔖₂(h)+b` from the target | ⚠️ **that fixes the first band's ratio at `1.13` by construction** — #132's shape for the fourth time (#132, #139, #150, this). ✅ Caught before the run, and the fix removes the need for `a` entirely: **the ratio of the two predictions is independent of it**, so the test compares capped against `n_c`-weighted and never needs an absolute scale. **The fourth instance is the first caught before execution**, which is what four recordings of a fault are supposed to buy |
| 178 | *(A2, open since increment 304, closed by the hazard named at 330)* the cell floor's **size** — `𝔖₂` overpredicting the closed form by `1.13×` to `1.82×`, growing with `N`, with the one named suspect tested and cleared | ✅ **it was a weighting fault in my own prediction, and hazard 9 is what pointed at it.** The closed form is `E[B] = Σ_c (n_c/n)·mean_{pairs in c}ρ(h)` — a **cell-size-weighted** average. Increment 312 built `E_same[𝔖₂]` by pooling `min(4n_c, 40000)` pairs per cell, so cells entered with weight `∝ min(4n_c, 40000)`: **capped**. The cap under-weights the large shallow cells, whose same-cell excess is smallest, which inflates the prediction — and binds harder as `n` grows. ✅ Measured, the weighting alone inflates by **`1.19, 1.30, 1.47, 1.57, 1.65, 1.70, 1.70, 1.69`** against an unexplained overshoot of **`1.13, 1.28, 1.43, 1.58, 1.65, 1.81, 1.81, 1.82`** — the first five agree to `3%`. ⚠️ And the mechanism explains the divergence too: the cap reaches **100% of the band's mass from band 6 onward**, so the inflation plateaus exactly where the two curves part. ⚠️ **A residual near `6%` at the top three bands is not accounted for**, so A2 is closed to within that, not exactly |
| 177 | *(hazard 9 swept across every ratio this program quotes over a band, rather than one at a time)* which of them have two values | ✅ **Two are exactly unambiguous, and they are the two that carry the most weight.** `𝔄(N) = V/W`, Proposition V's local factor and the `0.8106` of increment 309, gives **`0.00%`** between ratio-of-means and mean-of-ratios in every band; `Q(N)/N`, the squarefree density and the site of #133's normalisation fault, likewise **`0.00%`**. The half-normal arm `\|C\|/√V` sits at `0.56%`. ⚠️ Two are flagged: `ρ = C²/V` at **`3.10%`** at the bottom band falling to `0.47%` at the top — reproducing #175, which is the control and passes — and, newly, the Cauchy–Schwarz deficit `C²/(W·Q)` at **`9.10%`** falling to `0.83%`. ⚠️ **Stated rather than assumed: the deficit's ambiguity does not threaten increment 309**, because Proposition E's conclusion there is that `deficit/√N` is *bounded*, not that it equals a constant, and a `9%` band-level ambiguity cannot unbound a quantity measured at `0.888`–`0.961`. **The sweep's value is the two clean bills, not the two flags** |
| 176 | *(my own constant, inverted, and the absurdity of the result is what caught it)* increment 331's first pass, which reported the three `ρ` definitions differing by **172%** | ⚠️ **the half-normal relation is `E\|Z\| = √(2ρ/π)`, so `ρ = (π/2)(E\|Z\|)²`, and I multiplied by `2/π` — the reciprocal.** The whole "disagreement" was the factor `π²/4 = 2.467`. ✅ Caught because a `172%` spread between three estimators of one quantity is not a finding, it is a bug: `Z` would have had to be violently non-Gaussian, contradicting #284 and #290. **An implausible result is a debugging signal before it is a discovery**, and the ratio being exactly `π²/4` said which line to look at |
| 175 | *(hazard 9's first sweep, aimed at the quantity most claims rest on)* `ρ = Var C / V` | **it names three different quantities.** `A = mean(C²)/mean(V)` (a ratio of means, `reaudit_prop_E.py`), `B = mean(C²/V)` (a mean of ratios, `lab_mask_share_percell.py`), `C = (π/2)·mean(\|C\|/√V)²` (the half-normal arm of `verify_all.py`, and the source of #121's `0.810`). They coincide only if `V` is constant across the band and `C/√V` is exactly half-normal, and neither holds. ✅ Measured: they agree to **`0.75%` at `N ≈ 1.4·10⁷`** and differ by **`10.3%` at `10⁵`**, with a systematic ordering `A > B > C` (`B/A = 0.9855`, `C/A = 0.9632`) — `C` lowest because the mask makes `E\|Z\|` fall below a Gaussian's. ⚠️ **Consequence to fix rather than note: Proposition W is a statement about a ratio of totals, so it wants `A`; #121's `ρ = 0.810` is `C`, and in Proposition W's units it is `0.841`.** Increment 309's Proposition E derivation used `A` and is unaffected |
| 174 | *(the shape behind #167–#173, named)* **HAZARD 9: two summaries of one object are not comparable until each one's weight is stated** | Increments 326–329 spent four passes on a "contradiction" between `4.39` and `0.90`. Both numbers were **correct**. One was `Σ \|μ̂\|²\|Λ̂\|²` over atomic bins — implicitly weighted by `\|Λ̂(j/q)\|² ≈ (X/φ(q))²`, so `q = 3` counts `3600×` more than `q = 143`. The other was the **unweighted mean** of `\|μ̂\|²/Q` over 255 frequencies, of which only 22 sit at `q ≤ 11`. ⚠️ **Distinct from #30 and RV #3**, which were a quantity compared against a *target* at the wrong scale; here two *measurements of the same object* were compared against each other with different aggregations, and neither was wrong. ⚠️ It cost three invented mechanisms (#167, #168, #171), one of which — the first — was **right and discarded**. ✅ The rule: **an aggregate over a heterogeneous index must state its weight, and any comparison of two aggregates must state both.** Where the index is heterogeneous, report the weighted and unweighted forms side by side, as `lab_atoms_perq.py` now does per modulus |
| 173 | *(#167's refutation was itself the artefact, and #166 is restored with its mechanism)* "the Davenport mechanism is refuted: `\|Σμ(v)e(vj/q)\|²/Q = 0.949` against the coin's `0.998`" | ⚠️ **that measurement averaged away the effect it was testing for.** Per modulus the coin exceeds the real in the exponential sums by **`8.40×` at `q=3`, `15.16×` at `q=5`, `4.97×` at `q=7`, `3.60×` at `q=11`**, falling to `≈1` from `q=13` upward. Only 22 of the 255 frequencies sit at `q ≤ 11`, so an unweighted mean drowns them. ✅ **So `μ`'s exponential sums at small-denominator rationals ARE far smaller than a random sign sequence's — the classical major-arc cancellation — and #166's "`μ` suppresses the periodic covariance" is restored with that as its mechanism.** ⚠️ It is not a new fact about `μ`; it is this program measuring a classical one for the first time, and it took three wrong refutations to get there. ⚠️ The reconciliation is qualitative, not quantitative: periodogram and exponential sum agree in class and direction at every `q` but not within the pre-registered 30% at small `q` (`13.0` against `8.4`), the residual being leakage and the window mismatch. **#166 stands as a direction, not as a number** |
| 172 | *(the factor-5 contradiction of #169, resolved — and it was a summary statistic all along)* two measurements of one spectrum disagreeing by 5 | **they carried different weights and nobody said so.** The periodogram numerator is `Σ_atoms \|μ̂(f)\|²\|Λ̂(f)\|²`, and `\|Λ̂(j/q)\|² ≈ (X/φ(q))²`, so `q = 3` outweighs `q = 143` by `(120/2)² = 3600`. Every exponential-sum check reported the **unweighted mean** over 255 frequencies. ✅ Per modulus the two objects move together — both large at small `q`, both `≈1` from `q = 13` up — and `q = 3` alone carries **`41.2%` of the coin's atomic numerator against `9.0%` of the real's**. ⚠️ **Two summaries of one spectrum are not required to agree, and I compared them for three increments without stating which weight each carried.** That is the same species as #30 and RV #3: not a wrong number, a number compared against the wrong reference |
| 171 | ⚠️ *(three proposed mechanisms, three refutations, and the rule that follows)* the mechanism behind #170's `4.39×` | **refuted for the third time.** The candidate was the **even-`N` subsampling**: each periodogram bin aliases `f` and `f + 1/2`, and `Λ` lives on odd numbers so `Λ̂(f+1/2) ≈ −Λ̂(f)`, leaving `2·Λ̂(f)·Σ_{v odd}μ(v)e(vf)` — a coherent combination the coin's independent aliases cannot form. Measured on the odd-restricted sums: **`0.90×`**, against `0.89×` over all `v`. It changes nothing. ⚠️ Three mechanisms now — Davenport smallness (#167), the `√V` normalisation (#168), the even-`N` aliasing (this) — proposed by me and refuted by measurement. **The pattern says stop proposing mechanisms.** When every direct measurement of `\|μ̂\|²` says `≈1` and an aggregate says `4.39`, the likeliest remaining explanation is that **one of the two computations is wrong**, and the next step is to verify the periodogram against the exponential sum **at a single bin**, not to invent a fourth story. Recorded because inventing that fourth story is precisely how this program has generated its own corrections |
| 170 | *(the measurement that was missing from #166 through #169: a share is a ratio and I only ever looked at the ratio)* where the factor 5 lives | ✅ **in the atoms, not in the total.** Split absolutely for the first time: **numerator `coin/real = 4.390`, denominator `coin/real = 1.140`**, and they multiply back to the share ratio exactly (`3.850`, PASS at 1%). So the effect is in the atomic bins and the denominators agree to 14%. ⚠️ That kills the reading that the gap was in the window's total energy, and it means the per-frequency exponential-sum measurements — `1.06` at `j/q`, `0.91` at `j/(2q)`, `0.90` odd-restricted — are measuring something the periodogram is not. En route: the real puts `0.6%` of its energy in bins 1–1000 against the coin's `3.6%`, and `93%` against `71%` in the broadband rest |
| 169 | ⚠️ *(two of my own measurements of the same object disagree by a factor 5, and #166 cannot be quoted until they are reconciled)* the atomic energy share says the coin exceeds the real by **`5.155×`**; the per-frequency exponential sums say the two agree to **`1.06×`** at `j/q` and **`0.91×`** at `j/(2q)` | **the frequency correction was a real bug and it is not the answer.** The periodogram runs over even `N` with step 2, so a bin `b/n` is the `N`-frequency `b/(2n)` and the atoms sit at `j/(2q)`, not `j/q` — #167 measured the right quantity at the wrong frequencies. ⚠️ **But at the corrected frequencies the ratio is `0.91`, if anything favouring `μ`, and still nothing like `5.155`.** Since the spectrum of `C` is exactly `\|μ̂(f)\|²\|Λ̂(f)\|²` and the coin's is `\|ε̂(f)\|²\|Λ̂(f)\|²`, a share ratio of 5 with a per-frequency ratio of 1 requires `\|μ̂\|²` to differ from `\|ε̂\|²` **elsewhere in the spectrum**, and where is not identified. ⚠️ **Until this is resolved, #166's "`μ` suppresses the periodic covariance" is suspended, not withdrawn and not standing.** Left open exactly as stated, since guessing a third mechanism after two have failed is how this program manufactures its own corrections |
| 168 | *(the first suspect for #166, cleared)* whether the `2.1×` gap was created by the `1/√V(N)` rescaling — an `N`-dependent multiplication, hence a convolution in frequency, by a function whose own periodicity mod `30030` is strong | **it was not.** On the **raw** `C(N)`, with no rescaling at all, the coin already carries `5.155×` the real's atomic share; scaled, `5.210×`. The rescaling contributes about **1%** of the gap, and `1/√V` carries only `0.6%` of its own energy in the atoms. ✅ So `#166` is **not** scale-normalisation drift — this program's most frequent fault, and the right first suspect — and it survives at the level of `C(N)` itself. ⚠️ Recorded also because the raw share numbers themselves are new: the coin puts **`33.5%`** of its spectral energy in 255 atomic bins out of two million, which is `Λ`'s major arcs seen directly |
| 167 | *(a mechanism I proposed for #166 and the run refuted)* the candidate explanation — "at a rational `j/q` the prime side has a major arc while `μ`'s exponential sum is small by Davenport, and a random sign sequence has no such cancellation" | ⚠️ **refuted.** At the 255 atomic frequencies, `\|Σ_v μ(v)e(vj/q)\|²/Q = 0.9492` against the coin's `0.9975` — **`1.05×`, not the several-fold gap it would have to be.** `μ`'s sum there is `√Q`-sized, exactly like a random sign sequence; Davenport's bound is satisfied with enormous room and is not what is acting. ⚠️ The test is also not quite the right object — the spectrum of `Z = C/√V` is a windowed, `√V`-rescaled quantity and not `\|μ̂\|²\|Λ̂\|²` over the same range — so it refutes the mechanism cleanly and does not identify the true one. **Left open and stated as open, as #113 was** |
| 166 | *(two objects derived independently and never compared)* the **location mask**, from M.1's divisibility theorem, and the **atomic spectrum**, from the covariance — both living at the same frequencies | ✅ **They are exactly nested**: the cell-mean projection lies inside the atomic span at `containment = 1.0000` for both the real and the coin, so the comparison is like with like. ⚠️ **And the mask does not exhaust the atoms**: the atomic projection carries `1.52×` the cell projection's energy for the real. ⚠️ **But the surprise is the direction of the coin control.** The coin's atom-to-cell ratio is `16.87` against the real's `1.52` — **`11.1×` — and in absolute share the coin carries `14.78%` of its energy in the atoms against the real's `7.00%`.** The real's cell energy is `5.25×` the coin's, which is the mask behaving as it should; but its *non-mask* atomic energy is several-fold **below** the coin's. So `μ` **suppresses** the periodic covariance that `Λ` supplies, except at the moduli where M.1 creates one. ⚠️ That is the first place in this program where `μ` has looked *unlike* a coin in a direction that is not a defect in the estimator — and one run is not a finding |
| 165 | *(two more rules of mine that could not have passed, in the run that checked #164)* increment 325's `(M3)` and `(M4)` | ⚠️ **`(M3)` defined "non-atomic" as "outside my exact set"** — but the hypothesis puts atoms at **every** rational, and the exact set covers only `q \| 30030`, so the remainder still contains every atom with `q = 17, 19, 23, …`. It could not have passed, and its failure at `18.2×` is the hypothesis behaving correctly. ⚠️ **`(M4)` then thresholded where it should have classified**, asking whether each large peak sat within a bin of a small rational and failing `3/8`. ✅ Classified instead, the eight largest are: three **DC residue** (bins 1–3, after mean removal), three **leakage skirts** one and two bins off the `q = 3` and `q = 5` atoms the set already contains, and two **genuine atoms** — `8/23` to `0.043` bins and `3/17` to `0.118` bins, at exactly the moduli the exact set excludes. **Zero unexplained.** A threshold could not have said that; only a classification could |
| 164 | *(#162's headline overstated, and the run that found it was mine)* "the wall's fluctuation has a **purely atomic** spectrum on the rationals" | ⚠️ **that was a claim about density per bin, not about where the mass is.** Increment 324's `(P3)` measured `525×` per atomic bin, which holds; nothing in it said what share of the total mass the atoms carry. Summing the coin column of that same run gives **11.6%**. ✅ Measured properly: the sixteen exact moduli carry **`11.34%` of the coin's mass and `7.00%` of the real's**. That is not a refutation — the Ramanujan measure has infinitely many atoms and `Σ_q μ²(q)/φ(q)` **diverges**, so a finite set must carry a small share, and the share must accumulate like the partial sum. ✅ It does: over the twenty-seven exact moduli the cumulative measured mass tracks the cumulative `μ²(q)/φ(q)` with **increment correlation `+0.9110` and a ratio stable to `4.7%` across the last half**. **The atoms carry the Ramanujan measure; "purely atomic" is a statement about density and about the limit, and is not a statement about the 11%** |
| 163 | *(two faults in my own test of #162, and the run named both)* increment 324's first pass, which returned `corr = +0.30` and "the proposal is wrong as stated" | ⚠️ **(i)** the mass was summed over the `φ(q)` frequencies of each `q` and compared against a **per-frequency** weight — a factor `φ(q)`. ⚠️ **(ii)** it was tested on the **real** `Z`, which carries the location mask: a deterministic periodic function of `N mod 30030`, hence an atomic component at *exactly these frequencies*. The real spectrum is **mask atoms plus `𝔖₂` atoms**, and only the coin isolates the second — while the Ramanujan weights are a claim about the **covariance**, so the coin is where they must be tested. ✅ Corrected, the coin gives `+0.9864` and the real `+0.8425`, and the excess peaks at `q = 105 = 3·5·7`, a product of the very primes the mask lives on. **The failure located the mask's own spectral signature**, which no run had isolated before |
| 162 | *(the first law this program has for `C(N)` as a **function of `N`** rather than at a point)* the wall's fluctuation had a law for its marginal — `C = m + √V·G` with `G` Gaussian — and none for its **process** | ✅ **the fluctuation is a stationary process whose spectral measure is purely atomic on the rationals, with Hardy–Littlewood weights.** Increment 304 measured the covariance in closed form, `ρ(h) = a·𝔖₂(h) + b` at `corr = 0.9997`–`1.0000`; a covariance function *is* a process specification, and `𝔖₂` has the Ramanujan expansion `Σ_q (μ²(q)/φ²(q))·c_q(h)`, so the spectrum must be atoms at `j/q` with weight `μ²(q)/φ²(q)`. Tested on `n = 3.87·10⁶` even `N` chosen a multiple of `30030` so every such frequency lands exactly on a bin: **the atomic bins carry `525×` an equal number of others**, and on the coin the per-frequency mass tracks `μ²(q)/φ²(q)` at **`corr = +0.9864`** over sixteen moduli spanning four orders of magnitude in weight. ⚠️ **The coin reproduces it, so the dynamics belong to `Λ` through the shift, not to `μ`** — the fourth time a structure in the wall has turned out to be `Λ`'s. ⚠️ **It is a law, not a bound.** Increment 320 established that any route must supply a `(log N)^A` saving over trivial *pointwise*, and a covariance supplies none. The decomposition it suggests — split the wall over residue classes and bound each — reduces to a pointwise Siegel–Walfisz for `μ*Λ`, which is the parity problem again |
| 161 | *(a gate bypassed by shell semantics, one increment after building the gate)* the commit protocol `python code/lint_docs.py 2>&1 \| tail -4 && git commit …` | **a pipeline's exit status is the LAST command's**, so `tail` succeeding masks the linter exiting 1, and increment 321's follow-up **was committed and pushed with two live lint failures**. ⚠️ The failures were mine and harmless — check (H) flagging two paragraphs that merely sat beside a note mentioning Theorem D, fixed by narrowing its window to the line and the one before it, with the resulting blind spot stated in the source. **The bypass is the finding.** A linter that exits 1 gates nothing if the invocation discards that code, and every commit this session used that shape. ✅ Redirect to a file and test `$?` explicitly instead of piping |
| 160 | *(the question "could quantum uncertainty or entanglement help?", answered quantitatively rather than dismissed)* whether either is a resource against the wall | **both are already in this program's results, in their exact mathematical forms, and both are on the wall's side.** ⚠️ **Uncertainty is Parseval** — Heisenberg is a statement about a Fourier pair, and the pair here gives `sup_α\|S_μ(α)\| ≥ ‖S_μ‖₂ = √(6N/π²)`, which is Proposition E's route (ii): the inequality that **closes** the pointwise route. Measured: `‖S_μ‖₂/√N = 0.7797 = √(6/π²)` at every size from `2¹⁴` to `2²²`, and the slack `sup/‖·‖₂` sits at **`3.51`–`3.93` with fitted slope `+0.0005` — bounded, not growing**. ✅ **Saturating the uncertainty bound exactly would buy `3.79×`**, leaving the best conceivable pointwise product at `1.92 ×` trivial — *it saves nothing*, while the target needs `(log N)^A = 15.3` at `A=1` and `232.5` at `A=2`. ⚠️ **Entanglement has a precise counterpart**: `C(N)` correlates `μ` and `Λ` at a fixed sum, and independence would give `ρ = 1` exactly; Proposition W identifies the deficit as a prime-pair-weighted Chowla correlation, so **`ρ − 1` is this program's measure of it**. But increment 320 showed `ρ` and `𝔄` **cancel out of the bound-versus-target comparison entirely**, so a complete theory of it moves nothing. **Neither is a resource here** |
| 159 | *(one symbol, two unrelated quantities, both in the same document)* `ρ` | **`ρ` denotes Huang–Li's Lemma 1 function in Theorem D and D‴, and the cancellation ratio `Var C / V` in the wall's law.** At `N = 10⁸` they are `0.0137` and `0.810` — two orders of magnitude apart and unrelated. Both appear in `STATUS.md`: "the loss factor is `≥ 1/max_d\|ρ\|`" and, elsewhere, "`ρ = 0.810 ± 0.018`". Read together they give a loss factor of `1.23`; the truth is `≥ 73`, matching the `exp(√log N) = 73.11` recorded independently at 320. ⚠️ **Same species as the `𝔖`/`𝔄` collision of #74, #75, #83** — the singular series of the *count* substituted for the local factor of the *noise*. ✅ Repaired: Huang–Li's is now `ρ_HL` everywhere (nine sites across four documents), `NOTATION.md` records every symbol that has collided or could, and `lint_docs.py` check **(H)** flags a bare `ρ` within four lines of Theorem D's vocabulary. ⚠️ Its first run died printing its own findings — `cp949`, the em dash, **#122's defect in the linter that exists to catch such things** — and its second flagged the disambiguation sentences themselves, which would have made the fix unfixable |
| 158 | *(a prediction of mine missing a standard secondary term, and the shape of the miss said so)* increment 320's `(V1)`, "the Cauchy–Schwarz bound is `N√(6 log N/π²)`" | **it is `N√(6(log N − 1)/π²)`.** `W(N) = Σ_{n≤N}Λ(n)² ∼ N(log N − 1)`, since `Σ_{p≤N}(log p)² ∼ ∫log t dt = N log N − N`, and I used the leading term alone. Missed by 4%. ⚠️ **The ratio climbed monotonically toward 1** — `0.9572, 0.9598, 0.9620, 0.9641, 0.9658, 0.9674, 0.9689, 0.9692` — which is the signature of a missing secondary term and not of arithmetic hiding inside the bound; with the `−1` restored it is exact. Recorded because the run's own verdict text read *"the Cauchy–Schwarz bound carries arithmetic after all; Proposition E's reach would need qualifying"*, and that would have been the wrong conclusion drawn from a right measurement |
| 157 | *(increment 319's central comparison, made against the wrong reference)* "the margin is a power of `N` and the circle method loses the same power" | ⚠️ **a proof needs `bound ≤ target`, not `bound ≈ truth`.** 319 set Proposition E's loss `N^{0.498}` beside a margin `trivial/truth = N^{0.454}`; both numbers are right and the comparison answers *how lossy is the method*, which is not the question. In the target's own units, `\|C(N)\| ≪ N(log N)^{−A}`: **trivial sits `(log N)^A` above the target, Cauchy–Schwarz `(log N)^{A+1/2}` above it, and the measured truth a power of `N` below it.** ✅ So **the entire difficulty is a log power**, which is the parity problem in its own units — no method gives *any* saving over trivial for a Möbius-weighted sum. ✅ **And it unifies the two routes that 319 called "two different reasons"**: both must supply `(log N)^A` over trivial; the circle method instead *loses* `√(log N)`, and the divisor switch loses `exp(c√log N)`, beyond every fixed log power (Theorem D). Same currency, same failure. ⚠️ Note what does not appear: `ρ` and `𝔄` **cancel out of the comparison entirely** — the arithmetic of the wall's own scale is irrelevant to whether the method reaches the target, which is why sharpening the Möbius input cannot help |
| 156 | *(the attempt, and the honest place it stops)* whether the material this program has assembled adds up to an attack on `C(N) = o(N)` | ⚠️ **it does not, and the program's own theorems say why.** The measured truth is `C(N) ≍ √N(log N)^{0.29}` against a trivial bound `ψ(N) ∼ N`, so the **margin is `N^{0.454}` at `N = 10⁸` — a full power of `N`.** Measured `max\|C\|/N` runs `0.056, 0.038, 0.031, 0.020, 0.014, 0.010, 0.0082` across the octaves and is falling: **the pointwise truth is far below trivial and the gap is not the size of the extreme.** What is missing is a *pointwise method losing less than a power of `N`*, and this program has proved neither classical route is one — **Proposition E puts the circle method's loss at `N^{0.498}`, the margin's own size** (which is why "zero margin" is exact rather than rhetorical), while **Theorem D's loss is only `N^{0.233}` and it still fails**, because its obstruction is not a size deficit but the absence of mass at the truncation point. ⚠️ **Two routes, two different reasons, one wall** — and no third route is visible in the material |
| 155 | *(a new exact identity, and two bugs in my own check of it)* `Σ_{N} C(N)² = Σ_h M(h)P(h)` with `M(h) = Σ_v μ(v)μ(v+h)` and `P(h) = Σ_w Λ(w)Λ(w+h)` — the global form of Proposition W | ✅ **verified to `1.5·10⁻¹⁶`**, but only after the check failed twice. First it summed `C` over `[0, X]` against a complete `h`-sum, missing by 46% — `μ*Λ` has support to `2X` and Parseval makes the identity hold over the **full** range; the identity was right and the check truncated one side. Then it missed by `6.2%` at `2¹⁵` and `1.1%` at `2¹⁶`: **the circular autocorrelation of a zero-padded array already carries each lag at both `h` and `nf−h`**, and I doubled it again. ⚠️ The shrinking discrepancy is what sent me looking for a boundary effect rather than a double count — **an error that decays with `X` looks like an asymptotic and is not one**. ✅ What the identity buys is bounded and stated: a second moment gives almost-all `N` by Chebyshev, which is Chudakov–Estermann–van der Corput 1938, and no second moment gives a pointwise bound |
| 154 | *(a sentence written from a previous increment's numbers)* increment 319's `(U3)` output, which asserted *"sd below 1 means `Var(d)` **below** `sd_c²`"* | **that was increment 305's `0.9936`; this run gives `1.0528`, the other side of 1.** The claim was composed, not computed, from a number that was not in front of it. ✅ The direction is now derived from the run's own data (`Var(d)` is `10.8%` **above** `sd_c²`), together with the reason it decides nothing: at `n_eff = 0.8` the sampling error on a dispersion is far larger than that |
| 153 | *(#152 said the source of the bias was not identified; it is the standard error)* the closed-form cell variance's residual `+0.0204 ± 0.0029`, `z = +7.11` | **the algebra is exact and the standard error was wrong.** The numbers averaged were `24 × 8 × 32` cell-draws, and the cells within one draw are a **partition of the same realisation of `ε`** — not independent. Clustered by draw the same residual reads **`+0.0116 ± 0.0123`, `z = +0.95`**, and the measured effective sample size is **`n_eff = 0.8`**: six thousand cell-draws carry less than one draw's worth of independent information about this statistic. ✅ **The closed form is vindicated and every share computed from it at 318 stands with it.** ⚠️ Same species as #41: `std/√n` on samples that share a source. It is now the fourth time this program has written that, and the first time it was inside the tool the previous three increments depended on |
| 152 | *(a self-test that passed because it divided by the thing it was testing)* increment 305's check on the closed-form cell variance — *"the z it produces are standard normal, mean `−0.056`, sd `0.9936`"* | ⚠️ **that test cannot see a size-dependent bias in `sd_c`, because it divides by `sd_c`.** Increment 318 tested the same formula through `d² − sd_c²`, which does not normalise, and 16 coin draws give a residual of **`+0.0204 ± 0.0029`, `z = +7.11`**. The two are consistent: a per-cell error in `sd_c` cancels in the mean and sd of `z` while surviving in a sum of squares, where the large-`sd` cells dominate. ⚠️ **Source not identified.** ✅ Impact bounded exactly: a bias of `+0.020` in `d²` moves the share `d²/(d²+ρ)` by under **0.2%** where `d² ≥ 12.8` (the deep cells) and by up to **2.3%** where `d² ≈ 0`, so the per-cell shares below stand and the *count* of cells above 50% does not. **A self-test must not be expressed in units of the quantity under test** |
| 151 | *(`OPEN_QUESTIONS.md` A4 said "a share statistic that is not size-weighted — none is defined yet"; here is one)* the mask's share of the variance, which #67 quoted as `14.1%` falling to `1.15%` and which was withdrawn | **there are two questions and only one had ever been asked.** The pooled `B = Σ_c (n_c/n)(m_c−m̄)²` answers *how much of a **random** even `N`'s fluctuation is the mask* — small at large `N`, and correctly so, since a random `N` is in no deep cell. The per-cell `share_c = d_c²/(d_c²+ρ)` answers *how much of the fluctuation of an `N` **in that cell**  is the mask*, which is the question every use of the mask needs, since it is invoked precisely when `N` has many small prime factors. Measured across the octaves: pooled falls **`0.176 → 0.018`, a factor 9.6**, while the deepest cell's share runs **`0.981, 0.974, 0.978, 0.975, 0.966, 0.957, 0.946, 0.937` — a factor 1.05**. ✅ **The mask is not small, it is rare**, and the pooled statistic cannot tell those apart. At `N ≈ 1.4·10⁷`, for `N` divisible by `3·5·7·11·13`, **94% of the fluctuation is the mask** |
| 150 | *(a rule of mine that restated the rule above it, for the third time in nine increments)* increment 317's `(S2)`, "mean(`\|C\|/M`)·`√M` constant to 10%" | **that quantity *is* `\|C\|/√M`**, which `(S1)` already tests — the two columns agree to three decimals — so it was `(S1)` with a different tolerance, and the two duly returned **opposite verdicts on one set of numbers** (`PASS` at 3 SE, `FAIL` at 10.2% against 10%). ⚠️ Third instance of #132's shape: #132, #139's second half, and this. The tell each time is a column that tracks another one exactly. ✅ Replaced by the content `(S1)` cannot carry: **whether the constant drifts**. If `\|C\| ≍ √M(log M)^c` rather than `√M`, the ratio slopes against `log M` and nothing else in the run would see it. Measured slope `+0.0133 ± 0.0082`, `z = +1.62` — no drift |
| 149 | *(the last untouched item in Register B, and the note that asked for it was written 118 increments ago)* **RV #3**'s refutation of the SEAM formalization, whose own text says *"being over-strong and false is not evidence against the correctly normalized statement, **which has not been derived**"* | ✅ **It is derived now, and the corrected form is met with a power margin.** The seam object is `C = Σ_p μ(N−pk₁)μ(N−pk₂)` over primes in a dyadic band, `M` non-vanishing terms. As written SEAM demanded `\|C\| ≪ √M(log N)^{−A}` — a log saving over the **square-root** scale — and that is false. The chain consumes at the **trivial** scale (#30), so the correct demand is `\|C\| ≪ M(log N)^{−A}`, and `\|C\|/M ≈ 0.797/√M` beats every fixed `A` by a **power of `M`**. Same shape as #30 itself and as increment 309's Proposition E. ⚠️ **RV #3's own evidence was also the wrong number**: it quoted the half-normal constant as `0.717`, which is the **unconditioned** estimator that counts annihilated pairs as zeros — the fault increment 157 had already diagnosed. On the clean bucket `gcd(k₁k₂,N)=1` it is `0.7979` within 3 SE at every band, `max\|z\| = 1.91`. The refutation stands (an order-one constant is still not `(log)^{−A}`) but the figure quoted for it did not. **Register B is now empty** |
| 148 | *(RV #6's own measurement, given the spread it never had)* the round-1 table of the Cauchy–Schwarz loss `‖1‖₂(Σ\|D\|²)^{1/2}/\|Σ_k D(k)\|`, reading `58×, 45×, 957×`, from which "the loss is ≍ √K and affordable" was concluded | ⚠️ **the mean of that ratio does not exist, and two of the three numbers were single bands.** The denominator is a **signed** sum that passes through zero. Bootstrapped within each band: `mean/median` reaches **`4.7·10¹¹`** at `K = 2000` — one resample landed on a near-zero denominator — and a loss of `10×` the median has probability **≈5%** at every band, so the `957×` is the distribution's own tail and needs no explanation. ⚠️ The **single-band point estimate** — which is what the table reported — reaches **38× its own median** at `K = 700` while the medians run smooth (`7.5, 21.6, 39.2, 52.1, 63.4, 77.8`). ✅ **The conclusion survives on the median**: `median(loss)/√K` is constant to **9.3%** for `K ≥ 700` at `1.44`, so `loss ≍ 1.44√K` and RV #6's downgrade stands. The two smallest bands are pre-asymptotic and are reported as such rather than dropped |
| 147 | *(a summary table read instead of the body under it, in the document I wrote to prevent exactly that)* `OPEN_QUESTIONS.md`'s Register B rows for **RV #3** and **RV #6** | **RV #6 was settled at increment 199 and I recorded it as unresolved.** `CLOSURE_REAUDIT.md`'s *verdict table* says "in question", and forty lines below, its *body* says "**the mismatch is real as an observation and not fatal as an objection. Downgraded from refuted (major)**", with a measurement. I read the table. ⚠️ Same species as #134 — generalising from a summary I had assigned rather than from the source — and this time the source was **this file**. ✅ RV #3's row was right in direction but vague: its real open item is specific and is now stated as such — *"SEAM should be re-derived against the corrected target before it is called false; an over-strong conjecture that is false is not evidence against the correctly-normalised one"* — which remains the one genuinely untouched item in Register B |
| 146 | *(the one place this program did quote an SE for R4, and it is optimistic)* R4b's header, *"the block-ratio estimator's own SE is ~9% at B=8 and ~25% at B=64"* | **those are `√(2/nb)`, which assumes the block sums are iid Gaussian — and `D(k)` is neither, which is the thing R4 is testing.** Bootstrapped over the blocks: `9.8%` against `8.8%` at `B = 8`, and **`35.4%` against `25.0%` at `B = 64`** — an underestimate by 42% exactly where the criterion bites. ⚠️ **The direction matters: every R4 test is weaker than R4b's own numbers say, not stronger.** ⚠️ And the bootstrap cannot be used at `B = 512`, where it resamples four numbers and returns `31%` against an analytic `71%` — an estimator of an estimator, both starved by the same `nb` |
| 145 | *(#142 said R4's threshold could not be judged either; it can now, and the answer depends entirely on the block size)* **Forge R4**'s criterion `ratio(B) ≤ 0.5 × ratio(1)`, and its signature "ratio falling with `B`" | **R4 is a 5-sigma test at `B = 8` and carries no information at `B = 512`.** `ratio(B) = Σ_j S_B(j)²/used` is a sum of `nb = NK/B` squares, and with `NK = 2048` that is `nb = 256, 32, 4`. Measured relative errors `9.8%, 35.4%, ~71%`; the `0.5×` threshold sits `−5.30, −1.34` and (nominally) `−1.93` standard errors from the measurement. ⚠️ **The precision degrades as `√(2/nb)` — fastest exactly where R4's own signature would appear**, since a residue of the exact identity shows as a fall at *large* `B`. Same shape as #139: an estimator whose sample size **is** the parameter being varied. ✅ R4b's worry — "a mild deficit in the SAME direction at both `N`" — is answered: the recorded fall `0.9862, 0.9430, 0.7958, 0.5929` is **under 2 sd at every `B`**, and the reduced replication does not merely fail to reproduce it, it **reverses the sign at `B = 512`** (`2.52` against a baseline of `1.13`). The verdict stands at `B = 8` and is empty above it |
| 144 | *(a pre-registered rule of mine that would have discarded the finding that explains its own failure)* increment 314's rule (F1), *"if the reduced replication flags where the original did not, nothing else reads"* | ⚠️ **(F2) is computed from null draws only** — it never touches the real field — so it is readable whether or not (F1) holds, and when it fails it **explains** an (F1) failure instead of being invalidated by one. Written as it was, the rule would have thrown away the 8.8% false-alarm rate that accounts for the extra flag. ✅ Verdict logic restructured so an independent measurement is not gated on a dependent one. ⚠️ Also recorded: **2 of 6 levels were unusable at reduced `N`** ("insufficient grid"), so the reduction is partial and the per-level numbers, not the count, are the deliverable — a limit the first draft did not state |
| 143 | *(#142 said C4's threshold could not be judged; it can now, and it does not survive)* **Construction C4**'s criterion `defect_real ≤ 0.5 × defect_null`, from which "C-IV CLOSED" was recorded | **it is a `1.3`-sigma test, and pure noise satisfies it `8.8%` of the time.** C4 draws six nulls per level, averages them and discards the six, so the spread had never been seen; with `R = 40` draws the null reads `2.85`–`3.32` with sd `0.99`–`1.34`, putting the `0.5×` threshold only `−1.29 sd` below its own null. ⚠️ Over six levels the chance of **at least one spurious ALIVE is 42%**, so the recorded "0 alive of 6" had probability `58%` under pure noise and is **unremarkable**; the reduced-`N` replication duly flagged one level, at `−1.73 sd`, exactly as that rate predicts. ✅ **The DEAD direction is not undermined** — a trigger-happy test that did not trigger is still evidence of absence — **but the closure's stated basis is wrong**. It must rest on the measurement, `+0.10 sd` on average across levels and never systematically below the null, and not on the count of flagged levels |
| 142 | *(two kill-tests whose threshold judges a quantity they never print an error bar for)* **Forge R4** and **Construction C4**, both of which flag on a `0.5×` ratio | ⚠️ **neither can be judged, and one is under-powered exactly where its criterion bites.** R4 reports block ratios `0.9430, 0.7958, 0.5929` against a `B=1` baseline of `0.9862` with **no error bar beside any of them**, while **R4b's own header** puts the estimator's SE at *"~9% at B=8 and ~25% at B=64"*. At `B=512` the measured `0.5929/0.9862 = 0.601` sits near the `0.5` threshold with an SE that was never quoted. C4 is worse: defect ratios run `0.89, 1.27, 1.53, 0.89, 1.37` across `Q` against a threshold of `0.5`, and **the across-`Q` spread of ≈0.25 is the only clue to the sd** — putting the threshold about 2 sd below the null, which is marginal. ⚠️ **A threshold on a quantity whose spread is never printed is not a criterion, it is a wish** |
| 141 | *(hazard 6's third form, inside a kill-test)* **Forge R2**'s pre-registered rule, *"ALIVE iff mean G ≥ 2 or regression capture ≥ 2× the random control"* | **the ALIVE branch is unreachable as written.** The control came out **negative** — `R2_random = −0.00006 ± 0.00016` — and twice a negative number is *smaller*, not larger, so no measurement could have satisfied it. Part (a) separately returned `nan` (repaired for odd `k` in R2b). ⚠️ **A kill-test that cannot return ALIVE is the same defect as a verifier that cannot return FAIL**, and it sat in a closure for 134 increments. ✅ **The verdict nonetheless stands, on the measurement rather than on the criterion**: `R2_det = −0.00012` against a null of `−0.00006 ± 0.00016` is `−0.38 sd`. That distinction — which of the two a closure rests on — had never been drawn |
| 140 | *(a verdict that understates its own evidence by a factor 13)* **Forge R1**'s DEAD, recorded against the pre-registered rule *"ALIVE iff `R²_zeros ≥ 2 × mean(R²_random)`"* | **the criterion is an effect size and was never compared against the null's own spread.** The null is `R²_random = 0.2196 ± 0.0055`, so the `2×` threshold sits **`+39.9` standard errors** above it: as written, R1 promised only to exclude a *doubling*. Its measurement is `0.98×`, i.e. **`−0.80 sd`** — dead centre. ✅ So **the data exclude an enhancement of `7.5%` at 3 sd**, thirteen times sharper than the recorded verdict claims. ⚠️ The general fault: **ratio criteria in this program were chosen as effect sizes and never checked against the spread of the thing they threshold** — hazard 8 in a form the `verify_all` calibration did not cover, since there the target had a spread and no one measured it, while here the spread was measured and printed and no one compared the threshold to it |
| 139 | *(the same contamination as #136, twice more, in the run that measured the design's requirement)* increment 312's first economics measurement | **two faults, both mine, and the second is the interesting one.** ⚠️ First, `S_r = Σ_{k≡r} x_k` expands with a cross term `2δ·n_r·Σx`, so the requirement depended on the field's **realised mean** and the bisection searched only `δ > 0` — the same asymmetry as #136 one increment earlier. Centred, since a design cannot exploit a noise mean. ⚠️ Second, and it survived the centring: `mean_r S_r²` averages over **`d` classes**, so at `d = 2…6` it is an average of five or six numbers. The requirement came back **non-monotone — `d = 6` needing less than `d = 5`** — which no economics produces. **An estimator whose sample size is the parameter being varied cannot be read at small values of that parameter**, and nothing in the output says so unless it is checked. ✅ The analytic form with `c = Var₀/n_r` measured is used instead; the direct estimate is its check at `d ≥ 8`, where it settles to ratios `0.75, 0.72, 0.89, 1.09, 1.07` |
| 138 | *(the quantity #137 named as deciding every kill-test closure, measured for the first time)* whether K2's detection floor lies below what design K2 would need | **it does, overwhelmingly, in the regime the route can use.** Design K2 splits the `k`-average into progressions mod `d` at a Cauchy–Schwarz cost of a factor `d`, so a coherent shift must give `1 + n_r δ²/c > d`, i.e. `δ_design ≈ √(c·d(d−1)/n)` against a floor of `4/√n`. With `c = Var₀/n_r` **measured** at `0.516` the two cross near `d = 7`. ⚠️ **But beating the factor `d` is not the route's bar.** The corrected E1 target (#30) needs a saving of `(log N)^{2A+2}` over trivial, so the split must reach `d ∼ (log N)^{2A+2}` — a gain of a factor 6 is not a log power and cannot help the route whatever the design does with it. At the `d` the target does need: `A = 1` gives `d ≈ 8.0·10⁴`, `δ_design ≈ 3.4·10³`, **14,000× the floor**; `A = 2` gives `4.1·10⁶×`. ✅ **The first kill-test closure in this program to stand *quantitatively***, with its one blind region named — `d ≤ 6`, worth at most a factor 6, which the route cannot spend |
| 137 | *(what "DEAD" has meant in this program, and what it actually means)* every kill-test verdict, read throughout as **no structure** | **it means no coherent structure above a floor, and not one kill-test ever quoted the floor.** K2 flags at `\|mean\|/(sd/√n) ≥ 4` over `n ≤ 600` consecutive `k`, so its detection floor is `4/√n ≈ 0.163` standard deviations of the field. ✅ Shown rather than asserted: at twice the floor **all ten `h` values flag** (`z` from `+7.2` to `+9.8`), so the test can return ALIVE; unmodified it returns DEAD at reduced `N` exactly as the original did. ⚠️ **This changes what every kill-test-based closure says.** These designs are trying to beat a Cauchy–Schwarz cost of a factor `d`, so the gain they need is *small* — precisely the regime a floor can hide. **The decisive quantity is now named and is missing for every design**: how large a gain would make it ALIVE, in the same units as the floor? A floor of `0.163` sd is either comfortably below what the design needs, in which case the closure stands *quantitatively* for the first time, or above it, in which case the closure is empty. Twelve kill-tests still have no floor at all |
| 136 | *(a mis-specified reference in the same run, which failed loudly instead of passing vacuously)* the injection check's prediction `d* = 4·sd/√n` | **it omits the field's own mean.** The test turns at `\|mean + δ\| ≥ 4·se`, so `d* = 4·se − mean`; at `h = 64`, `mean = −0.0980` and `4·se = 0.2173` give `0.3153`, exactly the bisection's answer, against a prediction of `0.2173` — a 45% miss. **The injection was right and the reference was wrong.** ⚠️ Worth setting beside #132 one increment earlier: both were criteria I mis-specified, but that one was an **identity** and passed at `1.0000` while this one was **wrong** and failed at `1.45`. A criterion that can be wrong is worth more than one that cannot. ⚠️ And the corrected form is *itself* an identity — shifting `x` by a constant leaves `sd` unchanged, so the bisection solves the equation whose closed form I wrote — so it is now labelled in the source as a test of the **injection machinery** and not as evidence |
| 135 | *(the register was missing its most important column, and the omission was in its own opening sentence)* `OPEN_QUESTIONS.md` said a withdrawn **positive** claim "costs nothing but a claim", and tracked only the holes it left | **a positive claim used to derive something else takes the derivation with it, silently.** The derived statement is not withdrawn — it simply stops having support, and nothing in this repo tracked that. ✅ **Register C** added: for each of fourteen withdrawn positives, what was built on it and whether it survives. Mechanically, **35 citations across the live documents, 22 history pointers and 13 withdrawal sentences, none asserting a withdrawn number as its own** (`code/audit_withdrawn_dependents.py`). By reading: **#36**'s fitted variance law was replaced by the exact `V`, a better route; **#47** is not used by increment 309's derivation, which was rebuilt so `W` cancels; **#67/#69/#112/#119**'s exponents were never needed for the load-bearing claim, since "the mask does not threaten `C(N)=o(N)`" follows from `\|m\| = O(√V)` measured directly in units of `√V` (`−7.09` down to `−3.58`); **#84/#99** supported only a statement never claimed (Register A1); **#94/#96/#110** survive as statements about `Λ`. ⚠️ **Nothing load-bearing fell — but that sentence is worth exactly the reading behind it, which is one pass by the author of the claims.** Recorded so it can be checked rather than assumed |
| 134 | *(a property asserted of eleven files without opening them)* increment 308's Register B row: "Forge K1–K4, R1–R4; Construction C1, C2, C4 — all measured no signal against **permutation nulls**, which hazard 7 invalidates" | **false for twelve of thirteen.** Classified mechanically (`code/audit_killtest_nulls.py`, four self-tests): **exactly one** file contains a permutation null; **four already contain the coin control hazard 7 asks for** — `e1_constr_c1.py`'s own header reads *"Null: 8 draws of random signs on the real support"*; three use an analytic `z` with no resampling; one uses random-frequency surrogates; and five contain no null the classifier can see. ⚠️ So the row named the wrong hazard for almost every file in it: for the Construction group hazard 7 is **already satisfied**, and for the analytic group the live question is **hazard 8** (is the reference spread right?), not hazard 7. ⚠️ Sharper still, the fault is the one this program keeps recording — **I generalised across eleven files from a category I had assigned, not from the files** — and it appeared in the very document written to stop closures being forgotten. The row is now five rows, one per null type |
| 133 | *(a false FAIL from my own normalisation, in the same run)* the check "`Q(N)/N` reproduces `6/π²`" came back **`0.6448` against `0.6079`, off by 5.4%**, and was reported as a failure | **the arithmetic is right and the normalisation was mine.** `Q̄` is the band **mean** of `Q(N)`, and I divided it by the band's **geometric** midpoint `√(b·2b)`. For a band `[b, 2b)` the ratio of the two is `1.5/√2 = 1.0607`, and `0.6079 × 1.0607 = 0.6448` to four figures — the discrepancy was entirely that factor. Fixed by dividing by the band's own mean `N`; all eight bands then read `0.6079`. ⚠️ Same species as #30 and REVIEW_VERDICT #3: **a quantity compared against a target computed at a different point of the same band** |
| 132 | *(a criterion that could not come out false, written by me three increments after shipping a linter against exactly that)* increment 309's first quantitative test of the re-derivation: "`deficit/√N` equals `√(Q/(ρ·A))/√N` to 5%" | **the run returned a ratio of `1.0000` in all eight bands, because the two sides are the same expression.** With `ρ := mean(C²)/V` and `A := V/W` substituted, the right-hand side reduces algebraically to the left. It was an identity dressed as a measurement. ⚠️ **`lint_verdicts.py` cannot catch this**: the verdict *was* computed from data — it is the **criterion** that was tautological, and no linter in this repo checks criteria. The tell was the output itself, eight identical `1.0000`s. ✅ Replaced by the three things the derivation actually asserts, each able to fail alone: `A(N)` constant to 1% (measured `0.8106`, spread **0.00%**), `Q(N)/N → 6/π²` to 1%, `ρ ∈ [0.5, 1.2]` (measured `0.858`–`1.008`) |
| 131 | *(the lesson of #119, disregarded three increments later by me)* increment 309's first rule asked whether a power of `N` beat a power of `log N` in fitted RSS by a factor 10 | **it cannot, over this range, and #119 had already established that.** The fit gave exponent `+0.5133` — inside the pre-registered window — with the power model ahead by only `2.82×`. `#119` recorded, for this same `N` range, that a factor `160` in `N` does **not** separate `N^{−a}` from `(log N)^{−b}`; the log fit here wants exponent `7.23`, absurd as an asymptotic and perfectly adequate over two decades. ✅ Replaced by a **derivation** with no fit in it: `W(N)` cancels between the Cauchy–Schwarz bound and Proposition V's `V = W·𝔄`, so `deficit = √(Q/(ρ𝔄))` and the log powers are gone identically. **The right answer to "this range cannot separate two models" is to stop fitting, not to loosen the threshold** |
| 130 | *(the first re-derivation from `OPEN_QUESTIONS.md` Register B — and it corrects the register itself)* increment 308's triage put **Proposition E** first among closures at risk, as resting on "the size of `C(N)` against `ψ(N) ∼ N`", which `#47` changed | **it rests on no such thing, and the triage mis-assigned it.** Proposition E compares the two classical circle-method estimates against the **trivial bound**: route (i) `‖S_Λ‖₂‖S_μ‖₂ = √(W(N)Q(N))`, route (ii) `sup\|S_μ\|·‖S_Λ‖₁` with `sup ≥ ‖S_μ‖₂` by Parseval. Every quantity there is an exact sum over `n < N` or a consequence of PNT. **The true size of `C(N)` enters nowhere**, so no correction in the record touches it. ✅ And Proposition V **strengthens** it: `W(N)` cancels between `√(WQ)` and `rms C = √(ρ𝔄W)`, leaving `deficit = √(Q/(ρ𝔄)) ≍ √N` — measured `0.888 → 0.961` in units of `√N` across a factor 100. The method is short of `C(N)` by **a clean power of `N`, not a log power**; "zero margin" understates it. Route (ii)'s Parseval floor `‖S_μ‖₂‖S_Λ‖₁/N` measured `1.52 → 1.92` at `N = 2¹⁴…2²²`, never below 1. **Forge R5 rests on the same two lines and is settled with it.** ⚠️ Two closures cleared of thirteen; the eleven that rest on **permutation nulls** are untouched |
| 129 | *(the register that should have existed since increment 199)* this document records what was withdrawn and what replaced it, and **nothing recorded what each withdrawal left open** | ⚠️ **and the dangerous direction had never been looked at.** A withdrawn *positive* claim costs one claim; a withdrawn *negative* one — a **closure** — silently re-opens a route the program stopped looking at. This file was itself written to re-audit every closure against correction `#30`; **ninety-six corrections have been recorded since and no second pass was ever made**, increment 282 having checked exactly one of thirteen standing closures. ✅ `OPEN_QUESTIONS.md` now carries both registers: six holes with nothing in them, and a triage of which closures rest on a quantity a later correction moved. **The triage's result is a clean split — every closure at risk is a magnitude, normalisation or null closure; every safe one is structural** — the same division increment 300 found among the positive results, now applied to the negative ones. ⚠️ The triage is not the re-audit; the re-derivations are the work it names |
| 128 | *(a false positive cleared the wrong way, caught in the same run)* the withdrawn-form auditor's marker list, to which I added `wrong` and `premature` so that two legitimate historical mentions would stop firing | **loosening the rule silenced a real hit.** Those two words appear in the paper one paragraph above `b = 2.68`, about a *different* claim — the intercept — and the widened window then read them as withdrawing the rate. The genuine assertion vanished from the report. ✅ Both markers removed and the four legitimate mentions marked individually with `<!-- withdrawn-ok: reason -->`. **A false positive is answered with an explicit exemption that records its reason, never by relaxing the rule** — the relaxation is invisible and takes true positives with it. Two further faults in the same file were caught by its own self-test before it ran: `0.4891` matching the withdrawn `0.489` for want of a digit boundary, and case-sensitive marker matching that missed `Withdrawn with it:` |
| 127 | *(the rule "state the current position, do not argue with yourself" was enforced in one direction only)* whether any live document still **asserts** a form the record has withdrawn | **`paper/negative_map.tex` carried two of them as live measured facts** — the artifact a reader outside this program would open first. §"The wall's own law" stated `g = -0.489 ± 0.005` and `m(N) ≍ √𝔖(N)·N^{1/4}` (withdrawn at #112, again at #119), and the rate section stated `b` refitting to `2.6817` with "a drift of 0.048" (withdrawn at #300, where the estimator was shown unable to tell `μ` from a coin). The paper was written at increment 299 and **nine increments of withdrawals never reached it**. `LOCATION_MASK.md` likewise still quoted #67's `14.1%` and `1.15%`. ✅ Both passages rewritten from scratch, and the check is now mechanical: `code/audit_withdrawn_forms.py`, seven forms, five self-tests, exempting only `CLOSURE_REAUDIT.md` |
| 126 | *(a by-product of repairing `verify_deep.py`, and the sharpest confirmation of the mask yet)* the location mask at `N ≈ 10⁸`, where the FFT work of increments 300–305 cannot reach | **the two arms differ by `+0.2238 ± 0.0056`, forty standard errors.** With 300 deep `N` (`30030 \| N`) and 300 shallow (`3 ∤ N`) against the exact second moment, the half-normal ratio `r = \|T\|/√V` reads **`0.9476 ± 0.0293`** deep against **`0.7238 ± 0.0245`** shallow (40 seeds). The shallow arm agrees with #121's independent `ρ = 0.810` — `√(2ρ/π) = 0.718` against `0.7238` measured. ✅ The gap is now a **gated row**, the only one in this program that stakes a sign as well as a size, and it is what M.1 predicts: `n` prime and `q \| N` force `q ∤ (N−n)`. **The mask is alive at `10⁸`, a decade above where #118 resolved it** |
| 125 | *(a bias created by the repair itself, caught before it shipped)* the deep sample built as `sorted({…})[:n]`, which both `verify_deep.py` and `verify_all.py` used | **that takes the lowest `n` of the window, not a sample of it.** Harmless while the window was `3·10⁶` — a 3% span at `N ≈ 10⁸` — but 307 widened it to `3·10⁷` to fit 300 multiples of 30030, and the same line then concentrated the sample in the **bottom 30%** of `N`. The bias grows with the window, so widening the window is exactly what turns it into a defect. Replaced by enumerating the multiples and drawing with `rng.choice(…, replace=False)`. Recorded because the first calibration run was done with the biased sample and its numbers were discarded |
| 124 | *(a defect in the auditor of increment 306, found by pointing it at a second target)* `audit_stamp_calibration.py`'s check `(S)`, "identity rows have sd exactly 0" | **it reported FAIL, and the summary line said "an identity row varies with the seed", on a target that has no identity rows at all.** `verify_deep.py` has three statistical rows and no algebraic one; `okS` defaulted to `False` when `ident.any()` was false, and the verdict prose then asserted something that had not happened. ⚠️ **Counting an absence as a failure is not a verdict, it is a bug** — and it is the same species as #117 one increment earlier: a gate condition and the sentence it licenses drifting apart. Now reports `n/a`. The same pass made the failure rate recompute from **values against the current intervals** rather than the recorded exit codes, so a re-calibration no longer costs another 40 runs |
| 123 | *(hazard 6's third form, swept mechanically at last)* `verify_propositions.py`, which `CLOSURE_REAUDIT` #61 holds up as the **answer** to "a check that cannot fail" and `STATUS` cites as a reproduction command | **it printed `SOMETHING FAILED` and `A CHECK CANNOT FAIL` and exited 0.** It has the sensitivity block #61 credits it with, and no failure path at all. Two siblings carried the same shape: `lab_offdiag_chowla.py` (prints `SOMETHING FAILED`, exit 0) and `verify_deep.py`, which was a **verbatim pre-285 copy of `verify_all.py`** — criteria as characters inside output strings, no comparison, no exit — left behind when 285 repaired the original. ⚠️ **The repair was applied to one file and never swept**, for twenty-two increments. ✅ Now mechanical: `code/lint_gates.py`, seven self-tests, flags any file whose output announces a verdict while having no `sys.exit(≠0)`, `raise` or `assert`. Its own self-test caught it counting `sys.exit(0)` as a failure path before it ever ran on the corpus. Three files flagged, three repaired, corpus now clean |
| 122 | *(my audit script carrying the very defect the object it audits documents)* increment 306's calibration audit, whose 40 re-runs completed and were then **lost in the print step** | `UnicodeEncodeError: 'cp949' codec can't encode '—'` — the row names contain an em dash, and the script had no `sys.stdout.reconfigure(encoding="utf-8")`. **`verify_all.py` carries that exact fix in its own head, with a comment explaining why**, and I wrote the auditor without it. Nine minutes of computation reached the display layer and vanished. Two repairs: the reconfigure, and **the raw values are now written to `results/…_raw.json` before anything is printed**, so a display fault can never again destroy a run. ⚠️ This is hazard 6's sixth form — *a step whose record and reality diverge* — in a new place: not the record of a result, but the **survival** of one |
| 121 | the CI stamp's V1 interval `[0.60, 1.00]`, centred on the half-normal `√(2/π) = 0.7979` | **that centre is `ρ = 1`, which this program refuted at increments 288–289.** Since `r = \|T\|/√V` and `T` is Gaussian mean-zero on the shallow arm, `E[r] = √(2ρ/π)` with `ρ = Var T / V`. Across 40 seeds the shallow arm gives `r̄ = 0.7181 ± 0.0163`, which is **4.9 s.e. below** the half-normal value. ✅ Solving instead of asserting: **`ρ = 0.810 ± 0.018` at `N ≈ 10⁸`** — an independent measurement of the cancellation ratio a decade above the `1.6·10⁷` the FFT work reaches, and consistent with its slow decay. The deep arm reads higher, `0.8018`, because the mask adds a deterministic component there, so **the gap between the two arms is itself the mask's signature**. (Slight downward bias: `π r̄²/2 = (E√ρ)² ≤ E ρ`.) The row's centre now sits on measurement; the four rows with a surviving theory value keep theirs |
| 120 | *(hazard 8 pointed at this program's own gate)* `code/verify_all.py` passing, hence every claim it stamps | **the stamp fails on 25% of seeds.** Every row draws its sample from one seed, `default_rng(211)`, and no row's spread had ever been measured. Re-run under 40 seeds, the stamp as a whole exits 1 a quarter of the time — **the recorded green was luck**. Two rows sat barely inside their intervals: the shallow arm at **1.14 σ** (≈13% failure on its own) and E1 at **1.65 σ**. ⚠️ Increment 285 had repaired this file's *other* two defects and added a sensitivity block, but **a sensitivity block shows a test can fail; it says nothing about whether the interval is the right width**. ✅ Repaired: widths are now `centre ± 4σ` from the measured spread, centres stay on theory wherever theory survives, and the audit re-run gives **0% failure with margins 3.4–4.1 σ**, inside the pre-registered `[2, 20]` window. ✅ And the two standard errors this program had *derived* were both right — seam `0.0953` stated against `0.0904` measured, E1 `~0.15` against `0.1320`. **The error analysis was sound; the step that compares it to the gate's width did not exist** |
| 119 | *(the question #69 was trying to answer, asked properly)* the mask's decay was read off a falling column of eight numbers with no errors attached | **the decay is real and its law is not determined.** Fitted per depth on half-octave bands, weighted by the exact per-cell errors, a decay beats a constant by more than `2×` in weighted RSS at **5 of 6 depths**. But over a factor `160` in `N` the data do **not** separate `A·N^{−a}` from `A·(log N)^{−b}` — indistinguishable at depths 5, 4 and 0, with the power of `N` winning only at depths 3 and 2. ⚠️ And the fitted exponent **depends on the cell**: `a` runs `0.1434, 0.2152, 0.2713, 0.3686` across depths 5→2 (`b` runs `2.01, 2.93, 3.67, 4.92`), a spread near `4×` under **either** parameterisation. **There is no single mask exponent**, so #69's one `g` was fitting a mixture over cells as well as over a floor. Not replaced. Recorded because this program has twice fitted a power of `N` where a power of `log N` was right (#36, #47) and has never before reported the third possibility — that the range cannot tell them apart |
| 118 | increment 303's finding that the mask's **amplitude at large `N` is unresolved**, and its diagnosis that the coin floor was what hid it | **the mask is resolved in every band to `1.6·10⁷`, and the missing floor was never the main fault.** With the floor exact and the per-cell variance closed-form, `max_c\|z_c\|` runs **11.1, 10.9, 12.9, 12.6, 11.2, 10.3, 9.4, 8.4** — every band clears Bonferroni at 32 cells. The deepest cell `3·5·7·11·13\|N` sits at `−3.58√V`, **8.4 s.e. from zero**, in the top band. ⚠️ The real fault is that `B = Σ_c (n_c/n)(m_c − gm)²` weights each cell by its **size** while the mask lives in the **rare** deep cells — the largest-`\|z\|` cell holds `6.6·10⁻⁵` of the top band. Floor removed exactly, `B_mask` clears twice its error in only **2 of 8** bands. So **#69's statistic could not have measured the mask at large `N` whatever the floor**, and 303's "unresolved" was a statement about that statistic and not about the mask. **The aggregate is the wrong instrument for a signal carried by rare cells** |
| 117 | *(a self-contradiction my own run printed)* increment 305's first output line **"(A) exponent reportable (≥ 5 bands): PASS → g = not fitted"** | **two different statistics counted as one.** The gate was set on the number of bands resolved by the **per-cell z test** and then used to license an exponent fitted to the **aggregate** `B_mask`, which had gone negative in three bands and so was never fitted. Eight bands resolved, zero exponents fitted, and the summary line asserted both. Split into `(M)` on the per-cell test and `(A)` on the aggregate, which now read `8/8 PASS` and `2/8 FAIL` — the disagreement that turned out to be the finding (#118). `lint_verdicts.py` does not catch this: the string was an f-string carrying a computed value, so it was *computed* and still *wrong* |
| 116 | *(#113 answered in two of its three parts, and the third recorded as unclosed)* the coin's cell floor was called an unidentified property of "the estimator", with the note that "the coin has no arithmetic" | **the coin has no arithmetic in `ε`, but it has arithmetic in `Λ` — through the shift.** For a coin `Var(Z(N)) = V/V = 1` exactly, so the covariance is the correlation and is available in closed form with no Monte Carlo: `ρ(h) = (μ²∗g_h)(N)/√(V(N)V(N+h))` with `g_h(w) = Λ(w)Λ(w+h)`. Its numerator is a **prime-pair count at shift `h`**, so it carries the singular series `𝔖₂(h)`. ✅ **SHAPE**: `ρ(h) = a·𝔖₂(h) + b` with `corr(ρ,𝔖₂) = 0.9997…1.0000` in all eight bands and `\|b\| < 0.001`. ✅ **CAUSE**: two `N` in one cell agree on which `q ∈ {3..23}` divide them, so `h = N′−N` is divisible by small primes more often (`1/(q−1)` against `1/q`), where `𝔖₂` is larger. Permuting the cell labels across `N` — preserving every cell size, leaving `Z` byte-identical — collapses `B/T` from `0.053` to `0.0034` at the bottom band and from `0.038` to `0.00015` at the top, i.e. to the `(k−1)/n` of an independent sample. **The flatness in `n` is explained: the excess is a property of *pairs*, and no number of samples removes it.** ⚠️ **SIZE is not explained.** `E[B]/E[T]` in closed form (self-tested against 12 draws, max \|z\| = 1.7) runs `0.0582 → 0.0420`, while `𝔖₂` predicts `1.13×` to `1.82×` too much, and the overshoot **grows with `N`**. The named suspect — one `a` per band where `a` decays in `h` — was tested and **cleared**: the `h`-resolved model moves the ratios by under 0.05 (`1.12→1.13`, `1.77→1.82`) |
| 115 | increment 304c's **RULE B, "prediction within 1.5× of measured in 7 of 8 bands — PASS"** | **that pass was sampling noise.** The next run measured the target's own per-draw spread: **86.9%**, so at `R = 8` the standard error is ≈31% and a 1.5× tolerance cannot discriminate anything. Two independent 8-coin runs of the same band differ by a factor **2.4** (`0.0324` against `0.0781`). ✅ The repair is not more draws but removing them: `E[(Σ_{N∈c} Z)²] = Σ_v μ²(v)u_c(v)²` with `u_c(v) = Σ_{N∈c} Λ(N−v)/√V(N)`, one FFT per cell, exact. Judged against the closed form the same prediction fails, 3 of 8. **Hazard 8**: a pre-registered tolerance is meaningless until the target's own spread is measured — the rule looked strict and was not |
| 114 | *(a test of mine that could not have come out right)* increment 304's first attempt at #113's mechanism, which required `B/T` to agree with the mean pairwise correlation `ρ̄` | **the two can never agree, and the fault is in the test.** `B = Σ_c (n_c/n)(m_c − gm)²` is taken about the **grand** mean, so any component common to every `N` cancels exactly, while `ρ̄` is almost entirely that common component — measured `ρ̄ ≈ 0.20…0.46` against `B/T ≈ 0.06`. The right identity is `B/T ≈ ρ_same − ρ_all`, the **excess** correlation of same-cell pairs over generic ones, and stating it that way is what turned #113 into a question with an answer. Recorded because the run was pre-registered and failed both its rules, and the failure was mine and not the data's |
| 113 | *(an open question this run raises and does not answer)* the coin's between-cell variance is **`B/T ≈ 0.043` and does not fall with sample size** — `0.054` at `n = 5·10⁴`, `0.040` at `n = 1.6·10⁶` | **the cell means are not concentrating, and the mechanism is not identified.** Independent samples would give `B/T ≈ (k−1)/n`, i.e. `0.0045` and `0.00014` — falling by a factor 32 across the range and 290× below what is observed in the top band. So something makes `C(N)` within a cell behave like `O(1)` effective samples. A shared component common to all cells would cancel in a *between*-cell variance, so it is not simply the long-range correlation of #106. ⚠️ Left open and stated as open rather than guessed at; every candidate mechanism I can name fails one of the two observations (the size, or the flatness in `n`). Whatever it is, it is in the estimator and not in `μ` — the coin has no arithmetic |
| 112 | *(hazard 7 applied to the central positive finding, and it bites)* #67 and #69 quote the location mask's amplitude and its `N^{-0.489}` scaling from a **between-cell variance**, never compared against what a **coin** produces from the same enumeration | **the mask is real; its measured scaling is not.** Normalising each by its own total, the real signal runs `B/T = 0.226` down to `0.021` while the coin floor sits at `0.054` down to `0.040`. The ratio falls **4.21, 3.27, 2.66, 1.77, 1.35, 1.27, 0.80, 0.52** — crossing 1 at `N ≈ 6·10⁶`, and clearing every coin draw in only **2 of 8** bands. ✅ **M.1 is a theorem and the mask exists**; at small `N` the enumeration sees it plainly at `4.2×` the floor. ⚠️ But **#69's `g = −0.489` was fitted across all eight bands, of which the upper two are floor-dominated**, so the exponent — and with it `m(N) ≍ √𝔖·N^{1/4}` — is contaminated and withdrawn. **#67's "1.15% at `1.6·10⁷`" is below the floor** and is not a measurement. What survives: the mask exists, it is largest at small `N`, and its amplitude at large `N` is *unresolved*, not small |
| 111 | *(a seventh named hazard, and the one behind #106 and #110 both)* **a null that destroys everything identifies nothing** | Increments 294–295 and 288–297 each used a surrogate that scrambles the whole object — a permutation, a band mean. Such a null answers *"is there structure"*, which was never in doubt, and cannot answer *"which ingredient"*, which was the claim. **When the object is built from several arithmetic inputs, the null must vary exactly one of them and leave the rest byte-identical.** `C = μ * Λ`: to say anything about `μ`, hold `Λ` fixed and replace `μ`. Both coin controls took one FFT per draw and both could have been run at the increment that made the claim. Symptom to watch for: *a surrogate that is not itself a plausible version of the object.* A permuted `Z` is not a wall; `ε * Λ` is |
| 110 | *(the same control as #106, applied to the spectral claim, with the same outcome)* #94 and #96 read the zeta ordinates in `C(N)` as a property of **the wall**, against a **permutation** null | **the lines are `Λ`'s.** Replacing `μ` by a random `±1` on the same support, through the same `Λ` and the identical pipeline: real `R² = 3.896·10⁻³` against a coin mean of **`2.994·10⁻³`**, coin max `5.515·10⁻³`, and **6 of 20 coin draws at or above the real value**. The ratio is **1.30×**, not the `1566×` reported. `C = μ * Λ` and `Λ` carries the zeros by the explicit formula, so the lines were never evidence about `μ`. ⚠️ **Withdrawn**: #94's "the wall's fluctuation is Gaussian in distribution but not phase-random in `log N`" as a *property of the wall* — it remains literally true and is **empty**, restating that `Λ` is in the convolution; and #96's `0.39%` share, which is not attributable to `μ`. **Unaffected**: #91's detection of the zeros in `E(N) = r(N) − 𝔖(N)N`, which was always a statement about `Λ`, is confirmed by the explicit formula, and never claimed otherwise. ✅ Note what survives across #106 and #110 together: **every claim that `μ` behaves like a coin stands; every claim that `μ` is special has fallen** |
| 109 | *(#107 said its `z = −4.2` needed its own null; it has one now)* with **40** coin draws and the pre-registered rules, is the deficit in `E[C²]/V` real? | **no — and it is not measurable this way at all.** `0/8` bands past `−5σ`; `12/40` coin trends more negative than the real one; pooled, real `0.90589` against coin `1.00227 ± 0.12478`, **`z = −0.77`**, with **`9/40` coin draws landing below the real value**. Both clauses fail. The reason is structural and worth stating plainly: the estimator is **unbiased** (coin mean `1.00227`) but its per-draw scatter is **0.195**, because the `C(N)` across a band share most of their terms, so a band of `1.6·10⁶` values carries far fewer than `1.6·10⁶` independent ones. ⚠️ **So the low-variance estimator is biased (#106) and the unbiased one is too noisy: pooled deficit `0.094` against a coin scatter of `0.125`.** Proposition W's `ρ − 1` has **never been measured**, by any increment, and with one realisation of `μ` it is not measurable by either route. "How much the wall beats a coin" is currently **unknown** |
| 108 | *(an error in yesterday's correction, one increment old)* #107 reported the real second moment at **`z = −4.2`** below the coin in the top band | **that `z` divided by the standard error of the coin MEAN, not by the coin's spread.** Comparing a *single* realisation of `μ` against a null requires the null's standard deviation; the SEM answers "where is the coin's average", which is not the question. Recomputed against the distribution the number is **`z = −0.79`**, and with 40 draws `−0.72`. The direction #107 reported was right and its significance was inflated by `√R`. Recorded separately from #109 because they are different mistakes: this one is arithmetic, that one is structural |
| 107 | *(the estimator the coin does validate, and what it says)* if the centred `ρ` is biased, what is the right one? | **the uncentred second moment `E[C²]/V`, which is exactly 1 for the coin by construction — no centring, so no shared-term correlation is removed.** Measured: the coin gives mean **0.978** with trend **−0.003/band** (flat, as it must be); the real `μ` gives **1.008 falling to 0.858**, trend **−0.020/band**, with the top band at `z = −4.2` against the coin. ⚠️ **That is the opposite direction from #84** — under the estimator the coin validates, the ratio falls rather than rises. It is **not** established here: `z = −4.2` on one realisation is suggestive, the mask contributes to `E[C²]` for real `μ` and has not been separated, and this run was designed to test the pipeline, not to measure the ratio. What it establishes is only that **the earlier estimator was biased**; the sign of the real effect needs its own increment and its own null |
| 106 | *(the control this program never ran, and it fails)* every measurement of `ρ = Var C/V` has gone through one pipeline — FFT convolution, mask removal by modular cells, octave banding, dof corrections. **Does the pipeline itself depress the variance?** | **it does, and it accounts for the whole effect.** Substituting `ε(v) = ±1` at random on `{μ ≠ 0}` leaves `V` **identical** (`ε² = μ²`) and every other step byte-identical, so a coin must give `ρ = 1`. It gives **0.761, 0.780, 0.787, 0.792, 0.813, 0.828, 0.851, 0.860** — reproducing the real curve with `z` between **−0.5 and +0.4 in every band**. The pre-registered rule fails on both halves. **The centred estimator cannot tell `μ` from a coin.** ⚠️ Withdrawn: **#84**'s "ρ rises 0.760 → 0.837, the wall beats a coin by a shrinking margin" — the coin rises identically; **#99**'s `b = 2.68` and its stability, fitted to the biased quantity; and the *quantitative* half of **#86**, which compared a reconstruction against "the measured −0.18". **Proposition W's identity is untouched** — it is algebra — but nothing measured so far confronts it. The cause is visible: nearby `N` share the same `ε(v)Λ(N−v)` terms, so the `C(N)` are positively correlated across a band and subtracting the band mean removes real variance |
| 105 | *(a false positive in check B, and the fix for it)* the linter flagged `paper/negative_map.tex:560` — the English word **"floor"** at the start of a line after `…$1.051$--$1.068$ times the random-sign` | **the mid-formula test was wrong.** It asked whether the *last* `$` on the previous line was followed by text, which is true of **every** line ending in prose after an inline formula. A line is left open when its `$` count is **odd**, or when it ends on a brace, subscript or backslash. Replaced, with four cases in the self-test — the real fault, prose after inline math, plain prose, a trailing brace — because a check with a false-positive rate is a check that gets ignored |
| 104 | *(found while adding a check, by the mechanism the other check exists to catch)* increment 299 broke `lint_docs.py` while extending it | **the seventh escape collapse, and the most pointed.** A heredoc turned `\newtheorem` into a literal newline plus `ewtheorem`, producing a `SyntaxError` **in the file being edited to catch exactly this**. Restored from git and redone with the editing tool, which is what this program's own rule has said since increment 281. Recorded not because the repair was hard — it took one `git checkout` — but because *the convenient moment* is what keeps defeating the rule, and this instance had the least excuse available |
| 103 | *(a defect in the paper itself, invisible here because there is no TeX engine)* `paper/negative_map.tex` used `\begin{proposition}` **twice with no `\newtheorem{proposition}`** | **the working paper has not compiled since those propositions were added.** LaTeX raises *"Environment proposition undefined"* and stops; nothing in this environment runs LaTeX, so it went unseen. Fixed by defining the environment, and turned into **check G** of `lint_docs.py`: every `\begin{X}` in a `.tex` must have a matching `\newtheorem` or be a standard environment. Zero hits after the fix. ⚠️ Found only because increment 299 added a section that *used* the same environment and checked its references first — i.e. by luck, which is why it is now mechanical |
| 102 | *(#100 named a fault; #296's lesson says naming does not prevent it)* is "a conclusion that cannot be refuted by its own experiment" a one-off, or a class? | **a class, and now a mechanical check.** `code/lint_verdicts.py` parses every script's AST and flags a `print` of a **plain string literal** — no interpolated value — containing verdict vocabulary, where the print is not inside an `if`. Such a line asserts an outcome the run cannot contradict. It does **not** flag f-strings carrying a computed value (the number can refute the sentence around it), prints inside a branch (the branch *is* the computation), or lines marked `# verdict-ok: <reason>`. ✅ First run over 226 files: **10 candidates, 7 legitimate** — five *criterion* statements ("Pre-registered: ALIVE if …", fixed text by design) and two *structural* ones (true of the method regardless of data), each now carrying its reason in the marker rather than being assumed. **Three were real**, all mine: `lab_rho_rate.py`'s three outcome sentences (#100) and `lab_margin_extrapolate.py`'s "the exponent is not −1/2 … FASTER than predicted", where only the number was interpolated and the *claim* was typed — had `b3` come out below 0.5 the sentence would have contradicted its own figure. Both now derive the wording from the values. Self-test: five synthetic sources, each classified correctly |
| 101 | `lab_rho_rate.py`'s repaired conclusion was still typed out — the ladders `1.174, 1.088, …` and `2.7297, …` were **transcribed constants** in the prose | **a corrected hard-coded conclusion is still a hard-coded conclusion.** The fix at #100 replaced a wrong sentence with a right one and left the mechanism intact: change the data and the text lies again. Now every clause is computed — the verdict word from the pass/fail boolean, the RMS cost from the ratio, both ladders from the arrays, and even *which* coefficient is called steadier from `max−min` of each. The script now prints "the steadier coefficient is b (0.048 against 0.134)" because it measured that, not because I did |
| 100 | *(a conclusion written before the run, in the script that was to test it)* `lab_rho_rate.py`'s closing text asserted **"the theory-selected one-parameter model is not worse than the best free two-parameter fit"** | **the run rejected it, and the text said the opposite.** It was drafted alongside the pre-registration, describing the outcome I expected, and would have been read as a finding. Replaced by the actual result before anything was recorded. ⚠️ This is the same shape as the checks that could not come out false (#61, #71, #78) with the polarity flipped: **not a check that cannot fail, but a conclusion that cannot be refuted by its own experiment** — because it was already written. Rule: the verdict text must be *computed* from the outcome, never composed with it |
| 99 | increment 288 compared three unmotivated two-parameter models for `ρ(N)` and returned **INDETERMINATE**; nobody then asked whether the theory selects one | **Proposition W selects it, and the selection is falsifiable.** Taking `ρ−1 = (1/V)Σ_h c(h)S(h)` with `c(h) ≈ 𝔖₂(h)N` and `S(h)` at the square-root floor with random signs gives `Σ_h c(h)S(h) ≈ √N·N^{1/2} = N` against `V ≈ N log N`, hence **`ρ − 1 ≍ −b/log N`** — which is 288's best-fitting M2 with its intercept **fixed at 1** by Chowla, i.e. a **one-parameter** model where all of 288's had two. ⚠️ **Pre-registered rule: REJECTED.** `H_W` costs 34% in RMS (0.00652 against 0.00486), and M2's free intercept sits at `a = 1.039` — *above* 1 and **walking down**: 1.174, 1.088, 1.063, 1.070, 1.054, 1.039. So the data have not arrived at 1 and fixing it there is **premature at this size rather than wrong**; Proposition W predicts the limit, not how fast a finite window reaches it. ✅ **What survives is the rate.** `b` refits as 2.7297, 2.7218, 2.7115, 2.6885, 2.6819, **2.6817** — a drift of **0.048** across the whole window, settling. That is a measured coefficient; `a` moves 0.13 over the same bands and is still going |
| 98 | *(a hazard named five times and repaired locally five times)* escape collapse through shell heredocs had produced a truncated file, a corrupted correction table, two mangled Korean syllables, a syntax error and a broken formula — and each response was a note | **notes do not stop it; `code/lint_docs.py` does.** Six mechanical checks over all 28 `.md`/`.tex` files, exit 1 on any failure: **(A)** control characters — `TAB, BEL, BS, FF, VT, CR` are exactly what `\t \a \b \f \v \r` become when an escape collapses; **(B)** a line starting with a LaTeX command's tail (`mid`, `imes`, `symp`, …) when the previous line ends *mid-formula*, which is the only signature `\n` leaves since it collapses to a legitimate newline; **(C)** correction rows distinct and descending-by-one from the maximum; **(D)** STATUS's count against the table; **(E)** STATUS's increment against the last commit subject; **(F)** adjacent duplicate lines, which a botched splice leaves. ✅ **It found a live one on its first run**: `STATUS.md:294` held `$m(N)\x07symp…$` — `\asymp` with its `\a` collapsed to BEL, written by me at increment 291 and pushed ever since. **Sixth instance, and the first found by a machine rather than by accident** |
| 97 | *(an operational fault, not a mathematical one, and it clobbered a committed artifact)* increment 295's first draft was too slow, was moved to the background, and I rewrote and re-ran the script **while the old run was still going** | **the stale run finished after the commit and overwrote `results/lab_wall_spectral_share.txt`.** Caught because the notification arrived and the file was checked; nothing was lost, because `R²` is deterministic and only the surrogate statistics differed (200 draws instead of 100, maximum `5.09·10⁻⁶` instead of `4.16·10⁻⁶`). Resolved by adopting the **stronger** null — the script now runs 200 surrogates and every document quotes that run. ⚠️ But the near-miss is the point: **a background job writing to a tracked path can silently replace a committed result**, and the record would then disagree with the artifact while both looked fine. This is hazard 6's family — a step whose record and reality diverge — with a new cause. Rule: **a re-run of a script whose earlier invocation may still be live must write to a fresh path, or the earlier one must be stopped first** |
| 96 | *(#94 established the spectral component; significance is not size, and the law could not be stated without the size)* how much of the wall's variance is the zeros? | **0.39%, and that is a floor.** Regressing the de-masked, `√V`-standardised, band-standardised `Z(N)` on `cos(γ log N), sin(γ log N)` for the first ten ordinates gives `R² = 3.896·10⁻³` against a surrogate mean of `2.49·10⁻⁶` and a 200-surrogate **maximum** of `5.09·10⁻⁶` — **1566× chance**, established by the pre-registered rule. Every ordinate is individually significant: `z = 209.5, 95.1, 93.7, 51.9, 56.2, 57.1, 32.0, 36.4, 38.2, 23.4`. Amplitudes run `0.0644 → 0.0103` against the explicit formula's `2/\|ρ\| = 0.1414 → 0.0402`, i.e. the right shape at roughly a third the size. ⚠️ **0.39% is a FLOOR, not the share** — the zeros do not stop at the tenth, and quoting a truncated sum as a total is the same error class as #66. What it settles is that the component is **real and subdominant**, so Conjecture L's Gaussian half survives with a spectral term **named** rather than a new leading behaviour. No threshold was set on the share itself: the useful output is a number, and inventing a bar after seeing it would be hazard 2 |
| 95 | *(the null of #91 was not calibrated, and I found out by watching a surrogate fail)* increment 293 scored ordinates against a **local percentile** of the spectrum | **that null cannot be estimated from the window it uses.** The spectral resolution is `2π/span = 1.238`, so a `γ ± 4` window holds about **6.5 independent frequencies** — interpolating 4000 draws inside them adds no information, and a 99th percentile from six values is meaningless. The tell was a surrogate scoring **4/10** where a calibrated null gives ≈0.1/10. Replaced by a **permutation test**: the same statistic on 200 phase-randomised surrogates, calibrated by construction. ✅ **#91 survives recalibration** — `E(N)/√N` gives `S = 27.55` against a null of `10.52 ± 1.37`, **z = +12.4, p ≤ 0.005** — but it survives on a null that can be defended, which the original could not be. A conclusion reached through a bad null is not wrong; it is unsupported until the null is fixed |
| 94 | *(structure of a kind #74's distribution tests cannot see)* does the wall's own fluctuation carry the zeta zeros? | **yes, decisively.** `C(N)` de-masked and `√V`-standardised gives `S = 30.81` against a permutation null of `10.72 ± 1.44` — **z = +13.9, p ≤ 0.005**. Positive control: `M(N)/√N`, a *known* zero sum, scores `S = 33.46` (z = +15.8), so the statistic is calibrated against something whose answer is not in doubt. ⚠️ **This does not contradict #74.** Increment 283 established that the wall's fluctuation is Gaussian *in distribution* — excess kurtosis `−0.0005` — and a small oscillatory component leaves kurtosis untouched. **Gaussian in distribution and phase-random in `log N` are different statements, and this program had only ever tested the first.** Conjecture L's "no class structure" is a claim about the distribution; the spectrum is a second axis, and on it the wall is *not* structureless |
| 93 | *(a tempting identification, refuted)* `Λ` has mean 1, so `C(N) = Σ_v μ(v)Λ(N−v)` ought to be `Σ_v μ(v) = M(N)` plus zero corrections — and `\|M\|` is `N^{1/2+o(1)}` against the measured `\|C\| ~ N^{0.5457}` | **the wall is not the Mertens function.** `corr(C, M)` runs **0.066, 0.080, 0.052, 0.065, 0.060, 0.076, 0.062, 0.051** across octave bands — small, flat, and unchanged by removing the location mask. The pre-registered rule (`\|corr\| > 0.5`, not falling) **refutes** it. ⚠️ The exponents nearly agree — `β_{\|C\|} = 0.5272` against `β_{\|M\|} = 0.5137` — which is exactly the size-versus-identity trap increment 293 had to correct **in itself**; it was pre-registered here as "necessary, nowhere near sufficient" before the run, and the correlation is what decided it |
| 92 | *(two null-design faults of my own, both caught and both instructive)* the first draft of the spectral test above returned "lines NOT found", and its control returned the same two hits as the signal | **both were my errors, neither was a result.** ① The null pooled controls over the whole band `γ ∈ [8,60]`. The background is **not flat in γ** — it falls — so a peak at `γ = 14` clears a pooled 99th percentile merely by sitting where the background is high, and a genuine line at large `γ` cannot clear it at all. Replaced with a **local** null drawn from `γ ± 4`, which is what "stands out from the background" has to mean. That is hazard 4, a null set by a size heuristic rather than measured where it applies. ② The control `𝔖(N)C(N)/√N` was chosen as "something that cannot have lines" — **false**, since `C(N) = Σ_v μ(v)Λ(N−v)` contains `Λ`, whose own explicit formula carries the same zeros. A control has to destroy the **phase alignment** while keeping everything else, i.e. a surrogate, not another observable. Its 5/10 is a real signal, not an artefact — a bad control, not a bad result |
| 91 | *(a size argument replaced by a location argument)* increment 292 said `\|E\| ~ √N(log N)^{1.51}` "is exactly what the explicit formula predicts". **A size is weak evidence**: many things are `√N` times logs | **the sharp prediction is about where the peaks are, and it holds.** If `E(N) ≈ −2Σ_ρ N^ρ/ρ` then `E(N)/√N` carries `e^{iγ log N}` for every zeta zero, so its spectrum in `log N` must have lines at the ordinates `14.1347, 21.0220, 25.0109, …`. Binning `E/√N` on a uniform `log N` grid (span 5.075, resolution 1.238) and testing each of the first ten ordinates against a **local** null: **7 of 10** above their local 99th percentile, `γ₂` at **2.85×** and `γ₈` at 2.77×. The phase-randomised surrogate — same values, permuted across `N` — gives **0 of 10**. So the lines are at the ordinates and vanish when the `log N` alignment is destroyed. **This is the first time this program has detected the zeta zeros in its own data**; five days of `Λ` and `μ` statistics never touched them |
| 90 | *(three numbers from three increments that could not all be true, put side by side for the first time)* `\|R\| ~ N^{0.6458}` (#72), `\|C\| ~ N^{0.5457}` (#70), and `𝔖` bounded — yet `R = E + 𝔖C` exactly, so two pieces near `N^{0.55}` were summing to `N^{0.646}` | **all three are right, and the contradiction dissolves into two hazards this program had already named.** ① **Convention**: 282's 0.6458 is the *de-masked* exponent; the raw one is **0.6052 ± 0.0024**, and neither document said which — correction #81 (numbers measured under unrecorded conventions) recurring on a different object. ② **Effective vs asymptotic exponent**: a power fitted over a finite window absorbs logs. `β_E = 0.6061` at `log N ≈ 14.3` means **`\|E\| ~ √N·(log N)^{1.51}`**, exactly what the explicit formula predicts for a sum over zeta zeros; `\|𝔖C\| ~ √N·(log N)^{0.20}`. Reading 0.61 as an excess over square root is **correction #66 applied to this program's own numbers** — and the first draft of this script's decision rule did precisely that, declaring "a real excess over sqrt(N)" before the log power was solved for. ✅ What the run does establish: **`E` dominates `R`**, 60% at `N ≈ 10⁵` rising to **70%** at `1.6·10⁷`, so the Goldbach discrepancy is carried by the **classical error term, not by the wall** — increment 282's closure again, by a cleaner mechanism than the exponent comparison it actually used. Identity `R = E + 𝔖C` verified to `3·10⁻¹⁴` |
| 89 | *(a policy this repository states about itself, which I broke for eleven increments)* STATUS.md: *"The other documents carry no correction banners; they state the current position directly."* | **false since increment 280, and I made it false.** Sixteen `⚠️` banners accumulated outside `CLOSURE_REAUDIT.md` — CONJECTURE_L.md 3, MEASUREMENTS.md 6, STATUS.md 4, LITERATURE.md 2, LOCATION_MASK.md 1. The worst case: **CONJECTURE_L.md displayed `C(N) = m(N) + √(κ𝔖N log N)G(N)` and then denied it three times below**, across increments 280, 283 and 287. A reader reads the body, and the body was wrong. Appending a warning is easier than rewriting a paragraph, which is exactly why the rule exists. **Repaired**: CONJECTURE_L.md's `C(N)` section and MEASUREMENTS.md §12 rewritten from scratch to state the current law (`C(N) = m(N) + √(V(N))·G(N)`, no fitted parameter), with the superseded form named once and its history pointed at this file. CONJECTURE_L.md now carries **zero** banners. The four left in MEASUREMENTS.md mark *methodological cautions about live figures* (which convention a number used, that a cross-check is near-algebraic), not superseded claims, and STATUS.md's policy sentence has been rewritten to say that rather than to claim something untrue |
| 88 | the program quotes its margin on the wall as "nature over-delivers", using a **typical-`N`** figure, while the requirement `C(N) = o(N)` constrains **every** `N` | **the margin that matters is the one at the extreme, and it had never been stated.** With `max\|Z\| ≈ a_n` the extreme is `max\|C\| ≈ a_n·√(𝔄(N)·N·log N)`, so the margin over the requirement is `√N/(a_n√(𝔄 log N))`: `10^{2.2}` at `X = 10⁷`, `10^{4.4}` at `10¹²`, `10^{8.2}` at `10²⁰`, **`10^{22.8}` at `10⁵⁰`**. The requirement is not remotely tight — the entire difficulty is in *proving* `o(N)`, not in the size, and the documents should say the second thing rather than gesturing at the first. ⚠️ Scope, per #66: this is measured at `X ≤ 1.6·10⁷` and constrains nothing at the sizes where the no-go theorems bite |
| 87 | *(the tail, which none of #74, #84 or #86 touched)* the bulk of `C(N)` is Gaussian under `V` (#74) — but a bulk can match to five decimals while the tail is heavy, and **the tail is the only thing `C(N) = o(N)` constrains** | **the extremes are Gaussian too.** With the mask removed and `V` exact, `max\|Z\|` per octave band against the Gumbel law: mean deviation from `E[max]` is **+0.54 ± 0.45** over eight bands, consistent with zero. Aggregate tail counts against the Gaussian expectation: `t=3` **21441 vs 21463 (0.999)**, `t=4` **502 vs 503.6 (0.997)**, `t=5` 4 vs 4.6 (0.878). And the extremes sit at **generic `N`** — `2·8317`, `2·138917`, `2·32561`, … — not at deep radicals, so the mask removal is not leaking into the tail, which is what that column was printed to check. ⚠️ **My decision rule was mis-centred**: `a_n` is the *location* of the Gumbel limit, not its mean (`E[max] = a_n + γ·scale`), and centring on `a_n` turned a normal result into an apparent systematic **+1.12** excess. Recentred before the verdict was read |
| 86 | increment 288 left `ρ → 1?` unsettled and said more computing would not settle it | **identifying `ρ − 1` settles what it is, if not yet what it tends to.** Expanding `C(N)²` with `u = N−p`, `h = p′−p`: **`ρ − 1 = (1/V)Σ_{h≠0} c(h)S(h)`** with `c(h)` a weighted prime-pair count and `S(h)` the **binary Chowla correlation** (**Proposition W**). So the wall's excess over square-root *is* a prime-pair-weighted Chowla correlation, and Chowla-type input gives `ρ → 1` — **the wall is exactly square-root, over-delivering by a power of `log` and no more.** Three pre-registered quantitative tests, all passed: **(A)** `M(h)` sits at **1.051–1.068×** the random-sign floor `√(0.32264(X−h))` — not `√X`, since the sum only sees `n` with *both* `n` and `n+h` squarefree — stable across five decades of shift; **(B)** the reconstructed `ρ−1 = −0.0976` against the measured −0.17…−0.19, a factor 0.54 (bar: 3×; the gap is the dropped prime-power tail and the uniform-`u` approximation); **(C)** the sign is negative. ✅ **And the wall leans on the provable end of Chowla**: shifts `h < 10³`, where the conjecture is hardest and the averaged theorem weakest, carry **1.1%** of the gross mass; 48.9% sits at `h ≈ 10⁵–10⁶`. ⚠️ The ranges cancel 2.0× (net `−2.2·10¹³` against gross `4.4·10¹³`), so shares of the *net* would mislead and the first draft's table — which printed them, reaching 97.6% and −47.5% — was replaced |
| 85 | *(what is **not** settled, stated so it is not overclaimed in the other direction)* does `ρ → 1`, i.e. is the wall exactly square-root? | **not established, and the pre-registered rule says so.** Three two-parameter models on the same points: `ρ = c` (RMS 0.0258), `ρ = c(log N)^{−β}` (0.0060), `ρ = a − b/log N` (0.0049). The best is not below half of each rival, so the run returns **INDETERMINATE** — and every parameter still **walks**: `a` runs 1.174 → 1.088 → 1.063 → 1.070 → 1.054 → **1.039** across the window, `β` runs −0.501 → −0.286, `c` runs 0.782 → 0.810. ⚠️ **An upward drift is not evidence for 1 any more than a downward drift was evidence for 0** — hazard 5 applies symmetrically, and this entry exists because the previous one is the kind of finding that invites exactly that overclaim. What *is* established is the **direction**, and the direction says the extra cancellation is disappearing, not growing. ✅ Unforced cross-check: `sd(C) = √(ρV)` recomposes increment 281's independently fitted `β = 0.5457 ± 0.0032` as `0.5348 + 0.0100 = 0.5448`, difference **−0.0009** — different scripts, different fitted quantities, so this is evidence and not the algebraic identity flagged at #71. The decomposition also locates the exponent: **0.5348 of it is the arithmetic scale `√V` and only 0.0100 is the `ρ` trend** |
| 84 | the wall's cancellation ratio `Var C / Σ_v μ²Λ²` was recorded running **1.006 → 0.873**, read as the wall beating a coin by a growing margin | **the sign of that trend is wrong: it was the mask.** With the location mask removed by the same enumeration used since #67, the ratio **RISES**, 0.760 → 0.837, while the raw ratio falls 1.006 → 0.858. The two move in **opposite directions and converge** — gap 0.246 at `N ≈ 10⁵`, **0.020** at `1.6·10⁷` — exactly as finding #69 requires, the mask decaying like `N^{−1/2}` relative to the fluctuation. So `ρ < 1` is real and large (the off-diagonal is genuinely negative, the wall does beat a coin), but **the excess over square-root is shrinking with `N`, not growing**. The question only became askable at increment 287: until Proposition V the denominator in use was a fitted stand-in (#74, #75, #83), and a ratio to a wrong denominator has no direction worth reading |
| 83 | *(the sharpest correction of the session, and it explains three earlier ones)* this program has used `𝔖(N) = 2C₂∏_{q\|N,q>2}(q−1)/(q−2)` as **the** local factor everywhere, including for the wall's scale | **for the wall's scale it is the wrong function, by a factor of 760.** Increment 283 left `V(N) = Σ_v μ²(v)Λ(N−v)²` exact but opaque; expanding it gives a `(log p)²`-weighted count of **squarefree shifted primes**, whose local density is `1` at `q\|N` (since `q²\|N−p` forces `p=q`) and `1 − 1/(q(q−1))` at `q ∤ N` (a unit class mod `q²`). So the factor is `𝔄(N) = ∏_{q∤N}(1 − 1/(q(q−1)))` — Mirsky 1949, **recalled not claimed** (**Proposition V**). Measured on every even `N ≤ 1.6·10⁷`, with the analytic part removed *exactly* by dividing through `W(N) = Σ_{w<N}Λ(w)²`: `R/𝔄 → 1.000000` with sd falling 0.00154 → 0.000145, and per radical cell `R/𝔄 = 1.00000` to five decimals for all six cells tested. Rescaled to the same mean so only *shape* is judged: residual sd **0.000323** for `𝔄` against **0.245235** for `𝔖`. ⚠️ **The singular series of the problem is not the local factor of the noise.** `𝔖` is Hardy–Littlewood's, correct for the Goldbach *count*, and was carried across to the *variance* of `C(N)`. This one mis-identification explains #74 (93.7% of `(𝔖N)/V`'s variance is cell-explained — `𝔖` is the wrong cell function), #75 (`α` would not converge because a log-power was absorbing a wrong **arithmetic** factor), and why `κ ≈ 0.465` still fitted on average (`𝔖` and `𝔄` correlate) |
| 82 | *(a second error of my own, in the audit written to find the first)* the first draft of `audit_zero_support.py` explained why §6's dispersion figure 0.801 was undamaged by arguing that **"a number sitting at 0.80 cannot have been depressed by a large factor"** | **that assumes the conclusion.** It is the shape of reasoning this table exists to catch, written *inside the tool built to catch it*. Replaced with a measurement: the same family summed over **all `m`** — which is what `dispersion_engine.py` actually does — gives an annihilation rate of **0.0%** and `(a)/(b) = 1.0000` at both bands. §6's number is safe, and now for a checkable reason. Also reclassified: `e1_seam_law.py` is listed unguarded by the scan but is **instrumented** — its line carries `# 0 when killed` and the next statement *prints the annihilation rate*. The scan looks for filters and misses a published diagnostic, so `NO` in that column means "no filter", **not** "nobody noticed" |
| 81 | *(the fault of #80, swept across the corpus)* `abs(Σ)/√max(support,1)` is a **code idiom**, not a one-off, and this program's oldest recurring fault — a mask term read as a measurement — is exactly it | **37 sites in `code/`, 27 with no filter — but syntax is the wrong discriminator, and the right one is measurable.** The same statistic under three conventions, `N ≈ 10⁷`: over **primes `p`** the annihilation rate is **45%**, giving `(a) = 0.42` against `(b) = 0.78` — the fault is worth a factor **0.55**, and `(a)/(b) = 1 − (annihilation rate)` by construction, so **it is exactly as large as the mask is dense**. Over **all `m`** the rate is **0.0%** and the ratio is **1.0000**. So the idiom is harmless in dense summation ranges and severe in sparse ones; the static scan can prioritise but cannot judge. This also places §6's raw "0.348–0.370, stronger than random": it is the `(a)` convention, and §7's zero-accounting (`e1_zero_account.py`, convention `(b)/(c)`, giving 0.97–1.02) had already superseded it — **the correction was made; what was missing is that the documents never said which convention any number used**. `results/audit_zero_support.txt` now does |
| 80 | *(my own error, twice, inside the fix for #78)* the repaired stamp immediately failed on V4 (seam band, `r = 0.401` against half-normal 0.798), and **I diagnosed it wrong twice** | **first**: "the interval was set by analogy" — true, and hazard 4, but not the cause of 0.401. **Second**: "unlike V5, V4 pools the variance-suppressed shared classes" — plausible, documented in §6/§7, and **refuted by splitting them**: the shared class runs at `r = 0.808`, half-normal. **The actual cause**: the old line `abs(vals.sum())/sqrt(max(v,1))` fed **zero-support pairs into the mean as `r = 0`**. Those are M.3's predicted annihilations — pairs about which the law claims nothing — and counting them as *measured zeros* drags the average down. **45% of pairs (118 of 265) have support < 50**, and the arithmetic closes: `0.55 × 0.81 ≈ 0.45` against the old 0.401. This is the campaign's oldest recurring fault — **a mask term read as a measurement** — sitting in the CI stamp. The free/shared split is kept anyway, since matching V5's discipline is right and it is what refuted diagnosis two |
| 79 | V1 of the CI stamp drew its 30 test values as `rng.integers(...) // 6 * 6 + 2` | **every one of them is `N ≡ 2 (mod 6)`, so `3 ∤ N` always.** The location mask is largest exactly at `N` divisible by many small primes — increment 240 measured deep cells at −5 to −7 sd — and those `N` were **structurally excluded from the stamp**, which would therefore have passed unchanged if the mask were catastrophic. A **deep arm** (`30030 \| N`, so `3·5·7·11·13 \| N`) now runs alongside, and the two arms differ visibly: `r = 0.659` shallow against **`0.935` deep**. ⚠️ At 30 `N` per arm the standard error is ≈0.11, so that gap is ~1.8 s.e. — **suggestive, not established**, and it is recorded as the reason the arm exists rather than as a result |
| 78 | *(the fault this program named at increment 272, sitting in its own primary certification)* `verify_all.py` — what STATUS calls "the heavy CI stamp for the measurement corpus" | **it could not come out false.** Zero `assert`, zero `sys.exit`, zero `FAIL`: the criteria ("기준 0.80", "기준 1.0 ± 0.1") were **characters inside output strings**, not code, and the script exited 0 whatever the numbers were. Every V-stamp is now a `(name, text, value, pre-registered interval)` row judged mechanically, printing PASS/FAIL and exiting 1 on any failure, with a **sensitivity block** that pushes each value out of its interval and shows the verdict flips — asserting a check can fail is worth nothing (increment 276). Also fixed: the script died on `UnicodeEncodeError` under a cp949 console, which turns a stamp into an *exception* rather than a *failure* and erases the verdict entirely. ✅ **The one thing the audit expected to find was not there**: V1's `Σ log²p·[μ≠0]` and V5/V6's directly-counted support are **exact** second moments, so increment 283's `𝔖N` stand-in never reached certification |
| 77 | *(a prediction upgraded from "all observed" to exact)* M.3 — `q²\|gcd(k,N)` ⟹ `D(k) = 0` identically — was recorded as "1212 predicted, all observed" | **checked in both directions on 401,000 pairs and it is exact.** Predicted zeros 115,950; observed zeros 115,950; predicted-but-nonzero **0**; unpredicted zeros **0**. The second direction is the one that had never been tested: an unpredicted zero would be a *second* annihilation mechanism, and there are none. ⚠️ Sample note recorded in the output rather than left to be misread: every `N` on this grid is divisible by 4 (both `3·10⁶` and the step 2500 are), so `4\|gcd(k,N)` alone kills a quarter of the `k` and the 28.9% zero rate is a property of **this grid**, not of the field |
| 76 | *(the first time this audit found the campaign right)* Conjecture L's **original** half — `μ`-field = mask × exactly-Gaussian — was stamped at "kurtosis 2.99–3.03" on ~12,000 pairs, i.e. `±0.045`, while increment 283 used `±0.0017` to break the `C(N)` extension | **it survives at 26× the precision.** On **285,050** pairs: excess kurtosis **−0.0034 (z = −0.4)**, skew −0.0075 (z = −1.6), `E\|Z\|/sd = 0.79760` against `√(2/π) = 0.79788` (**z = −0.2**), variance ratio 0.99781. **No class structure** either: `gcd(k,N)` splits give `\|z\| ≤ 2.5` over four classes, variance ratios 0.978–1.020, consistent with sampling. **And the reason this half was safe is instructive**: repeating it with a *band-mean* support in place of the exact per-`k` count gives excess kurtosis **+0.4645 (z = +50.6)** and `E\|X\|/sd` off by −13.1σ, because support varies 40.4% across `k`. §7 counted the zero terms directly and stepped over the trap; the `C(N)` extension used a fitted `𝔖N` scale and fell in. **Same program, both halves, and the difference is whether the exact quantity was available *and used*** |
| 75 | *(a puzzle dissolved rather than solved)* increment 280 could not identify `α` in `Var C = κ𝔖N(log N)^α` — it walked with every fitting window and no reachable `N` would settle it | **because there is no `α` to identify.** `κ𝔖N(log N)^α` is a **fitted stand-in for an exact quantity**: the wall's scale is `V(N) = Σ_v μ²(v)Λ(N−v)²`, which needs no fit at all. The stand-in matches `V` in the *mean* at reachable `N` (`κ log N ≈ 6.5` against the measured `V/(𝔖N) ≈ 6.05`), which is why the fit looked fine, and gets the *fluctuation* wrong, which is why the exponent would not converge. ⚠️ **Increment 238 already said this** — *"the normaliser that needs no fitting is the exact second moment"* — and the conjecture was then written with `𝔖N` anyway, and three later increments spent effort fitting an exponent of a quantity that was never the right object. **A fitted law standing next to an exact one is a choice, and this program made it without noticing** |
| 74 | Conjecture L claims the wall's fluctuation is **"exactly Gaussian … kurtosis 3"**, and the campaign displays `C(N) = m(N) + √(κ𝔖(N)N log N)·G(N)` | **the claim is true and the displayed formula is false; they use different normalisers.** Every even `N ≤ 1.6·10⁷`, cell means removed, band standardised: under the displayed `𝔖N`-based scale the excess kurtosis is **+0.1704 (z = 98.1)** and `E\|X\|/sd` misses `√(2/π)` by −0.0062 (z = −29). Under the **exact** `V(N) = Σ_v μ²(v)Λ(N−v)²` it is **−0.0005 (z = −0.3)**, with `E\|X\|/sd` off by −0.00018 (z = −0.8) — Gaussian to a precision this program has never previously reached, on 6.3·10⁶ values, needing cell **means** only. A depth ladder rules out mask leakage (going from primes ≤ 11 to ≤ 31 removes **−3.3%**, i.e. nothing); per-cell standardisation removes 101%, and the reason is that `(𝔖N)/V` varies with sd 0.049 about a mean of 0.165 with **93.7% of that variance cell-explained**. Increment 240's recorded "+0.014 after masking" was measured under `V` and is right; every document that displays the `𝔖N` form inherits a normaliser under which the conjecture's own central claim fails |
| 73 | the criterion statistic behind that closure, `corr(C,D)`, was reported as **0.60 pooled** across five groups, and the supporting exponent as `\|R\| ~ N^{0.599}` | **both are wrong, and one of them in the direction that flatters the closure.** Pooling across bands of different scale *attenuates* a correlation; per band the de-masked value is **0.80 falling to 0.71**, and pooling every band of the full census reproduces the same artefact (0.7064). The true correlation was never as low as 0.60. Removing the location mask **raises** the correlation by **+0.042** — the mask was *suppressing* the association, the opposite of the natural guess that a shared deterministic term inflates it. Corrected exponent: **`\|R(N)\| ~ N^{0.6458 ± 0.0065}`**. Independently, `β_C = 0.5461 ± 0.0033` here reproduces increment 281's `0.5457 ± 0.0032` by a **different estimator** (`E\|·\|` de-masked vs `sd`), which is the one cross-check in this pair that is not forced |
| 72 | *(a recorded CLOSURE re-opened)* §9 refuted "the ln 2 comet and the final scalar are the same object" on a pre-registered criterion, citing `\|R\| ~ N^{0.599}` against `\|C\| ~ N^{0.503}` | **the closure stands; the evidence recorded for it did not.** Full census of every even `N ≤ 1.6·10⁷`, mask removed: `β_R − β_C = +0.0997 ± 0.0034`, **29 s.e.** — R really does outgrow C. But replicating §9's own design 500 times (five groups of 80 *consecutive* even N, offsets varied) gives that difference as **+0.079 ± 0.054**, so the recorded gap 0.096 was **1.8 s.e. of that design** and **7.0% of replicates return a difference ≤ 0**. A refutation was recorded on evidence that would have come out the other way one time in fourteen. ⚠️ **What actually makes the closure safe is the one thing §9 did not check**: the de-masked correlation **falls**, `−0.0198 ± 0.00035` per unit `log N`, moving *away* from the 0.9 threshold. §9's five noisy groups appeared to show it *rising* (0.457, 0.746, 0.771, 0.831, 0.726); had that been real, the closure would have been premature. **Right conclusion, inadequate evidence, and the adequacy was decided by luck** |
| 71 | *(a check I nearly shipped, caught before the run finished)* increment 281's consistency test compares the amplitude exponent β against the one implied by §12's variance exponent α | **that comparison is near-algebraic and confirms nothing.** β is the slope of `log sd(C)` on `log N`; α is the slope of `log sd(Z)²` on `log log N`; and `sd(C) = sd(Z)·~√(𝔖N)`. They are **the same fitted numbers in two parametrisations**, so `β = 0.5 + (α/2)(Δloglog/Δlog)` holds by construction — and the run duly returned agreement to **6·10⁻⁴** against α = 1.30, which reads like strong corroboration and is worth nothing. **This would have been the fifth check in this program that could not come out false.** The output now says so *above* the numbers. The one informative row is `α = 0`, which the old law predicts and the data refute; a genuinely unforced diagnostic is `E|C|/sd = 0.79145` against the Gaussian `√(2/π) = 0.79788`, a consistent **−0.81%** with the sign expected from the residual excess kurtosis |
| 70 | `\|C(N)\| ~ N^{0.503}` — **"square-root, to three digits"** (§9, and the headline of STATUS §3b) | **the precision is refuted and the exponent is not 0.503.** §9's design is five groups of **80 consecutive even N** (each group spans 160 in N); a scale from 80 samples has sampling CV ≈ 7.9%. Re-running **that exact design** 500 times with varied offsets: **β = 0.516 ± 0.0426**, 5th–95th percentile [0.447, 0.585], and the recorded 0.503 sits at **percentile 39** — an ordinary draw. The replication spread is **43× the 0.001** that "three digits" asserts. Full census of every even `N ≤ 1.6·10⁷` with the mask removed gives **β = 0.5457 ± 0.0032**, so `β = 1/2` is **excluded at 14.5σ**, and the mask alone shifts β by +0.014. `\|C(N)\|` grows **measurably faster than √N** — which is what §12's own variance law requires, and §9 was read as confirming the opposite. **Nothing about the program's position changes**: the requirement was only `o(N)`. What changes is that a number quoted to three digits had three digits of noise |
| 69 | *(a finding, not a correction)* the location mask's **own** scaling in N had never been measured — only its shape at fixed N | **`Var_mask(Z) ∝ N^g` with `g = −0.489 ± 0.005`, walking `−0.443 → −0.489` as the window grows — i.e. `g → −1/2`, so `m(N) ≍ √𝔖(N)·N^{1/4}`.** The value is still drifting but **the sign is not** (~100σ), and the sign is what matters: the mask is **lower order than the fluctuation** and therefore does **not** threaten `C(N) = o(N)`. With `E₃` already known to cancel 82% of it in the Goldbach count, the mask is harmless twice over — a real feature of the wall, not an obstruction. It also explains the bias in #67 mechanically: a term whose variance share falls like `N^{−1/2}` must bias a fitted log-exponent downward by an amount that shrinks with the range, which is why the raw and de-masked fits close from 0.70 apart (3 bands) to 0.29 apart (8) |
| 68 | the wall's variance exponent has been quoted as `log N` to the first power, with `κ ≈ 0.465`, in CONJECTURE_L.md and MEASUREMENTS.md | **the exponent is not identified, in either direction.** Fitted `α` **walks with the window**: 0.83→1.02 raw, **1.60→1.30 de-masked**, neither converged, both drifts ~10× the standard error. ⚠️ The de-masked fit reports `z = +8.58` against `α = 1` — **quoting that as ‘α=1 excluded’ would have been a false ALIVE of exactly the kind this table exists to catch** (hazard 5, turned on the estimate instead of the null). Worse for the form: over this range a pure power `N^ε` fits **as well or better** (residual ratio 0.45 raw, 1.91 de-masked, against a bar of 2 fixed before the run), so the log factor is *consistent with* the data, not *shown by* it. **What survives is `α ≠ 0`** — the variance grows faster than `𝔖N`, which is #36's actual content. And it will not be settled by computing: `log log N` spans 0.33 over a factor 160 in N, so halving the standard error needs `N ~ 10¹⁰` |
| 67 | the variance law was fitted at increments 236–238; **the location mask was found at increment 240** — two increments later — and the law was never re-fitted | **the fit was contaminated by a term this program itself discovered.** `sd(G)` is `g.std()`, which removes one band-wide mean and nothing else, so `m(N)` sat inside the measured variance throughout. Removing it by the same modular enumeration that defines it (cells keyed by which of `{3,…,23}` divide `N`, per-cell means subtracted within each band, exact `n−k` dof correction so the removal cannot shrink the variance by itself): the mask supplies **14.1% of the variance at `N≈10⁵`, 1.15% at `1.6·10⁷`**, and because that share *shrinks*, leaving it in **tilts the exponent by 0.29** at full range and 0.70 on the first three bands. Found by checking commit dates, not mathematics — **a measurement is only as current as the last thing discovered before it** |
| 66 | *(an observation nobody asked for, and it reframes every no-go here)* from what N does Theorem D′ actually **bite**? | **N ≈ 10⁴⁸⁰ at Lichtman's level, and further out below it.** Solving `c√(η log N) = A log log N` (c=1, A=3): `η=0.40` crosses at `N≈10⁴⁸⁰`, `η=0.10` at `10³⁰⁷¹`, `η=0.01` at `10⁵³⁷⁴⁴`. Below those the switch route loses **nothing that matters** and the theorem is silent. Two independent solves agree (required η at `10⁵⁰⁰` is 0.388, just under 0.40; the 0.40 crossover is `10⁴⁸⁰`, just under `10⁵⁰⁰`). This does not weaken any no-go — they are asymptotic statements and are true — but **they constrain METHODS, not any computation anyone will run**, and the documents were easy to read as though they bit at reachable N. Now stated wherever they appear |
| 65 | the prose under Theorem D′ said closing the gap *“would require **θ_E = 1 exactly** — … each progression holds **O(1) terms** and the statement carries no information”* | **wrong on both halves.** The theorem itself is correctly hedged (*for each **fixed** θ_E<1*); the prose then skipped the regime `θ_E = θ_E(N) → 1` **at a rate**. Solving instead of gesturing: `exp(c√(η log N)) ≤ (log N)^A ⇔ η ≤ (A/c)²(log log N)²/log N` (**Proposition D⁗**), so the boundary is `1−θ_E ≪ (log log N)²/log N` — **strictly stronger than EH, strictly weaker than θ_E=1**. And that regime is **not vacuous**: progressions retain `exp(C(log log N)²)` terms, which beats every fixed power of `log N`. ⚠️ This prose stood from increment 196 and was used at increment 235 as **one of three grounds for closing the program's highest-value open item**. The closure survives — Lichtman's `3/5` is a fixed constant and reason 1 (object mismatch) is independent and decisive — but *“vacuous”* says the target is meaningless where *“stronger than EH”* says it is meaningful and out of reach, and only the second leaves a well-posed question |
| 64 | *(a robustness result, not a correction)* Theorem D rests on one unconditional estimate, Huang–Li's Lemma 1 `ρ_n(x) ≪ e^{−c√log x}`, which invites the objection that the no-go **records our ignorance and would dissolve if the estimate improved** | **it runs the other way, and the proof is one line.** The paper already displays `|B_w| ≤ ‖b‖₁·max_d|ρ_{dN}(K/d)|` (`theorem_A.tex` l. 833), so the loss factor is `≥ 1/max_d|ρ|` — **monotone decreasing in any upper bound for ρ**. Every improvement of Lemma 1 strengthens Theorem D; none can weaken it (**Proposition D‴**). Since `1/ζ` enters to the first power, RH gives `ρ_n(y) ≪ y^{−1/2+ε}`, and `K/d ≥ N^{1/2+δ}` turns the conclusion from `exp(c√((1/2+δ)log N))` into **a power of N** — at N=10⁵⁰, 3·10¹² in place of 44. `lab_rho_decay.py` measures ρ's decay directly (discrimination rule fixed before the run): deep n give **H_pow** decisively (RMS 0.081 vs 0.202 at n=2310), shallow n come back INDETERMINATE **because ρ changes sign** there — the signature of a ζ-zero-governed tail, not regime ambiguity. ⚠️ The fit is **not** evidence that β=1/2 is the truth and the deep-n cleanliness is plausibly pre-asymptotic; **the robustness conclusion does not depend on the fit**, only on the monotonicity |
| 63 | LITERATURE.md's placement table said Theorem D's new content was making the classical genre precise *“for this reduction, **under full EH**”* | **wrong, and it misstates the theorem in the direction that flatters it.** Theorem D's ingredient list is **Bombieri–Vinogradov at `Q = N^{1/2−δ}`**; full EH is what **Bombieri's asymptotic sieve** assumes, not what Theorem D assumes. The two were conflated because increment 233 placed Theorem D “in the genre of” Bombieri's sieve and never compared the hypotheses. Laying them side by side shows they **do not overlap**: Bombieri's sieve is positive, at level 1, and needs the weight to obey parity; Theorem D is negative, at every `θ′ < 1`, and quantifies over all weights. Found only because Proposition D‴ required reading both statements at once — **a claim about the literature had gone 45 increments without anyone re-reading the theorem it described** |
| 62 | *(a check, not a correction)* the (18) defect report has been held back on the reasonable ground that **the authors may already know** | **checked, and there is no sign that they do.** arXiv:2005.03811 has exactly two versions, v1 (2020-05-08) and v2 (2022-08-28); v2's revision note is *"title changed, typos corrected"*, not a mathematical correction, and it is the latest. The paper is a Springer book chapter (CANT 2020, doi 10.1007/978-3-030-67996-5_17). No erratum and no citing work noting the issue was found. So the step stands uncorrected roughly four years on. **Caveats**: absence in a web search is weak evidence for a book chapter, whose errata are poorly indexed, and the authors may know privately. **Nothing has been sent** — that is the author's decision, and the check is recorded in the report's own header so it travels with it |
| 61 | *(a structural response, not a correction)* four times in this run a verification was shipped or nearly shipped that **could not come out false** — the silent heredoc patch (259), the algebraically forced identity line (272), the audit's own false alarms (274), a hardcoded `True` inside the verifier (275) | **asserting that a check can fail is worth nothing; showing it is worth something.** `verify_propositions.py` now re-evaluates each identity with one side perturbed by 1 part in 1000 and requires the verdict to flip. All five do, each residual landing ≈1000× its tolerance (M.2, P.1, P.3b: 10.95 against 0.011; P.2, P.3a: 206.5 against 0.207). The four counting checks test `violations == 0`, which a single injected violation flips by construction. **A verifier nobody has run against a known-false input is not known to work** |
| 60 | *(an audit that came back clean, plus a fault in the audit itself)* were the load-bearing figures now standing in the documents transcribed correctly from `results/`? | **yes — 24 of 24, and 3 derived figures recomputed within tolerance.** Transcription is a fault class that nothing else here would catch: a number can be measured right, recorded right, and quoted wrong. ⚠️ The audit's **first run reported 8 false alarms out of 24** because the documents typeset the minus as U+2212 and often quote a rounded form, while the check compared raw ASCII substrings — **the very fault class the tool was built to find, in the tool**. Fixed by normalising the sign and accepting any rounding. Stated limit, in the output: it checks that a figure was not invented or mistyped, **not** that it is the right figure for the claim it supports |
| 59 | *(a derivation, not a correction)* the cancellation nulls used throughout §4 and increments 267–269 model `D_p` as **independently signed** terms | **the signs are forced.** Proposition P.7: with r the least prime not dividing N, for `(N−1)/p < r²` the terms of `D_p` are `v = p` with `μ = −1` and `v = qp` with `μ = +1` — **one negative against a sum of positive**. Verified with 0 genuine violations in ~70 000 checks, the 21 exceptions all being the `N−v = q^k` case P.5 states. So "how far from random" is the wrong question at these p; the right one is the balance `pos/neg`, which reads **0.021, 2.61, 7.63, 19.52** at 1, 2–3, 4–8, ≥9 terms for deep N against 0.558, 2.08 for a typical one. Cancellation lives only in the narrow window where that ratio passes 1 |
| 58 | *(a derivation, not a correction)* does P.5's floor refute P.4? | **No.** Corollary P.6: the floor is `2A(N)N/(√δ·log N)` with `δ = φ(rad N)/rad N` — predicted 0.2577 against a measured 0.2699 for the deep group. That is `Θ(N/log N)` typically and `Θ(N√(log log N)/log N)` at maximal depth, **`o(N)` in every case**. Depth raises the floor by `1/√δ` = 2.28, 2.88, 4.05, 4.96 as `rad N` runs over the primes up to 13, 10², 10⁴, 10⁶ — unbounded but very slowly growing, and never breaking. It also **derives §5's ceiling in general**: §5 got `C(N) ≍ N/log N` from `j = 1` alone, P.5–P.6 get the same order from every `j` at once |
| 57 | *(not a correction — a derivation that closes increments 264–268)* the depth effect on P.4 was measured but unexplained | **Proposition P.5**: `Λ(N−v) ≠ 0` forces `(v, rad N) = 1` (M.1), so with `v = mp` the nonzero terms of `D_p` are exactly those with `(m, rad N) = 1`, giving `(φ(rad N)/rad N)(N/p)` of them. The floor on P.4's demand therefore scales as `√(rad N/φ(rad N))` — **2.284** at `N = k·30030` against ≈1.41 typically. Measured floor ratio **1.69** against a predicted **1.62**. And at deep N every class with `j = ⌊N/p⌋ ≤ 15` has `S/M = 1.0000` exactly, because `m = 1` is the only `m ≤ 15` coprime to 30030: **39% of the demand is single-term and structurally irreducible** |
| 56 | *(a pre-registered prediction of mine, refuted in direction)* the deep-N excess in `Σ log p\|D_p\|` sits at **large p**, since `D_p` is a single forced-sign term there | **it sits entirely at small and middle p.** By band, deep N gives `S/S_sign` = **14.39** at p ∈ [16,32] falling through 10.03, 7.17, 4.01, 2.59, 1.42 to **exactly 1.0000** above p ≈ 3·10⁴; all even N shows no excess anywhere (0.84–1.04). The reasoning failed because a **one-term sum has ratio exactly 1** under a sign-randomised null — the null uses the same magnitude, and sign is irrelevant to a single absolute value. Two consequences: **the deep-N excess IS the mask**, at precisely the p where increment 263 found the removable part; and for `p > N/2` the statistic is **structurally blind**, which is the same fact as §5's ceiling `Σ_{p>N/2}Λ(N−p)` |
| 55 | two specification faults in this program's **own** depth ladder (increments 264–266) | **(a) the null.** `R_null = Σ log p √V_p` is the right random-sign level only where `D_p` has one term; where it has many, `E\|D_p\| = √(2/π)√V_p = 0.798√V_p`. Replaced by a **sign-randomised** null — same support, same Λ weights, μ replaced by random signs — which needs no formula and handles every p. It comes out **close to the old one** (the mass sits at large p, where the one-term case applies), so the correction is only **+5 to +8%**: `R/R_sign` = 0.959, 1.324, 1.442, 1.500, 1.551, **1.584** against the old 0.882 … 1.522. **Every conclusion of #52 and #54 stands**, with the ratios slightly larger. Draw-to-draw sd 0.0001–0.0010. **(b) the labels.** The ladder's rows are *N divisible by the core*, hence **nested supersets** and lower bounds on depth — and the first row is **every even N**, not a depth-0 class. Increments 264–266 wrote them as depth classes |
| 54 | increment 264's depth ladder, whose deepest row had **n = 2** and which left open whether R reaches 1 | repeated at X = 2·10⁷ with **n = 10 per depth**. The two axes separate: **R decays −13.3% on average per factor 12.5 in N at fixed depth**, while the depth steps shrink to **+0.025** by depth 7. Along the primorial sequence a depth step multiplies N by the new prime, so depth 6 → 7 costs **−15.4%** in N against **+3.6%** in depth — **the N-decay outruns the depth growth**, and the measured pair agrees (0.7817 at depth 6, N ≈ 10⁶ → 0.7069 at depth 7, N ≈ 1.9·10⁷). P.4's demand decays even along the hardest sequence. What stays refuted is the **premise**: `R/R_null` is flat in N at fixed depth (1.522 → 1.537), so square-root-per-p is not what carries deep N |
| 53 | *(a pre-registered calibration that failed, recorded because the proxy would otherwise have been used)* the depth ladder restricted to `p ≤ 10⁵` as a cheap stand-in for the full statistic | **it does not earn its use.** The criterion was that the two depth trends agree. Direction and monotonicity do agree, but at depth 6 the proxy reads `R/R_null = 3.073` against the full statistic's **1.522** — an exaggeration of about 2× — and the proxy's own value shrinks with X (R at depth 0 falls 0.176 → 0.065 from X = 1.6·10⁶ to 2·10⁷), so it is not comparable across N either. It may be read for **direction only**; the quantitative question, whether R reaches 1, needs the full statistic |
| 52 | transform P's margin premise: **square-root cancellation per p**, measured as `S_abs/S_null` **flat at 0.87** | flat only **on average over N**. Along the primorial ladder at N ≈ 10⁶ the ratio climbs **0.959, 1.030, 1.234, 1.363, 1.433, 1.488, 1.522** as rad(N) takes 0 to 6 odd primes: at deep N the per-prime sums cancel **worse than random**, because the location mask gives each `D_p` a systematic component. `R = S_abs/Σ log p M_p` climbs 0.391 → 0.782 with `R = 1` being no cancellation at all. The increments shrink, so R may saturate below 1, but the last row has **n = 2** and the decisive measurement — R along the primorial sequence at N ≥ 10⁷ — is not done |
| 51 | Proposition **P.4's margin**, reported as `S_abs/N ≈ 0.30 and falling` | that is an **average over all even N**, and P.4 is a statement about **every** N. Split by depth in the location mask at N ≈ 10⁶: shallow `N = 2q` gives **0.2576**, deep `N = k·30030` gives **0.7773** — three times the reported figure, with the all-N average sitting near the shallow end because deep N are rare. Not a new obstruction (the deep group declines at the same relative rate: 0.8137, 0.7695, 0.7488 across a factor 3 in N), but the margin claimed in TRANSFORM_P §4 is the margin at a typical N and not at the hardest. Also measured and negative: **de-masking does not help** — replacing `Σ log p\|D_p\|` by `Σ log p\|D_p − m_p\|` gives **1.7677** against a raw 0.7773 (permuted floor 1.1114), because the mask lives at `p ≤ 8192` while the demand's mass is at large p |
| 50 | *(an audit that came back clean, recorded because a clean audit is a result)* after #48–#49 it was a live question whether the **negative map's kill-tests** shared the counting fault | **they do not.** `sweep_A` and `sweep_B` count both the mean and the sd-ratio z; the forge kill-tests K2 and K4 report 0 flags with every printed |z| ≤ 1.8, so their two-flags-required rule never had to discard anything; `hyp_round2b` reports max |z| = 1.92 against a null of 0.798 mean |z|. The closures stand. **One caveat**: in K2 the third ALIVE criterion `|ρ₁| ≥ 0.15` is set to `NaN` when fewer than 50 consecutive-k pairs survive, which happens at h = 30 and h = 210, and `abs(nan) >= 0.15` is False — so *could not be evaluated* was scored as *no signal* at two of eight h. The other two criteria did evaluate there and the verdict is not in doubt, but the record should say two of three, not three of three |
| 49 | `sweep_B2`'s verdict, from **3 of 5** flags surviving `÷𝔖` | the same omission as #48 — `sweep_B3` was derived from this helper, so both dropped the three printed mean-z. Counting all eight: **6/8 raw, 4/8 after ÷𝔖**, the added survivor being the `ω≥5` **mean at z = −5.25**. Read together with #48 the pair now says something clean: **√𝔖 is the scale mask** (all three sd ratios → 1.040, 0.998, 1.007), **𝔖 overshoots** (they invert to 1.287, 0.708, 0.820), and **neither touches the location** (mean −6.35 after ÷√𝔖, −5.25 after ÷𝔖). `sweep_A` and `sweep_B` were checked and count both z, so their verdicts are unaffected |
| 48 | `sweep_B3`'s verdict "PARTIAL — 𝔖(N) explains some of the structure", from **1 of 5** flags surviving `÷√𝔖` | the counter saw **5 of the 8 statistics it printed**: `stats()` computed the three mean-z, printed them, and appended only the sd-ratio z to the flag list. Counting all eight gives **6/8 raw and 2/8 after ÷√𝔖**, and the two survivors are `corr(·,𝔖) = −5.34` and the **ω≥5 mean at z = −6.35 — larger than any counted statistic**. Both are location; all three sd ratios collapse to 1.040, 0.998, 1.007. The sharper verdict is **√𝔖 removes the scale structure completely and leaves the location structure untouched** — the fifth independent sighting of the mask in the old sweeps, and the only one where the evidence was printed and simply not counted |
| 47 | *(no prior claim — a new measurement, recorded here because its first fit was wrong)* the ratio `Q = Σ C² / Σ_v μ²(v)Λ(N−v)²` decays like `(log N)^{−0.62}` | **`(log N)^{−0.43}`.** The first fit ran across all eight bands including two with `Q > 1` — the pre-asymptotic regime, where the wall is *less* cancelled than random and no asymptotic model applies. Fitting a power law through a curve still turning over inflated the exponent by 45%. On the `Q < 1` bands the decay still beats a constant (weighted residual **71.4** against **405.3**), so the decay is real, but it is slow: `Q ≈ 0.68` at 10¹² and `≈ 0.27` at 10¹⁰⁰. **No sub-square-root cancellation**: `C(N) ≍ √N·(log N)^{0.29}` |
| 46 | MEASUREMENTS §13: the off-diagonal of the wall's variance identity is **positive and comparable**, "about half the variance of C(N) is genuine shifted-Möbius correlation" | **it is negative.** The reading rested on X = 2000 and 4000. Reproduced there (0.554, 0.498) and then continued: `off/LHS` = **0.077** at 10⁵, **−0.144** at 10⁶, **−0.208** at 4·10⁶, crossing zero near X ≈ 3·10⁵. The shifted-Möbius correlation **removes ~14% of the diagonal** rather than supplying half the total. Separately, the same identity's diagonal measures `diag/(n·N·log N)` = 0.751 → 0.806 → A(N), so §13 **already implied** `Var C ≍ N log N` while §12 asserted `𝔖(N)·N`: the document contained its own refutation for fifty increments (see #36) |
| 45 | MEASUREMENTS §12: sweep_B's five flags are "all what a 𝔖-dependent **scale** predicts", so "the mask is **√𝔖**", with the leftover **mean drift** "already insignificant at N ≈ 9·10⁵" | **four of the five are scale; the fifth is location.** A scale mask cannot correlate C with 𝔖. At full power (n = 1.95·10⁶ against sweep_B's 1500) `corr(C,𝔖) = −0.1386, z = −193.5`, and sweep_B's own fix `C/√𝔖` still reads **z = −137.4**; exact scale removal `C/√V` leaves **−211.8**. Dividing by √V flattens the sd across 𝔖 bands (0.88–0.94) while the mean runs 0.012 → **−1.682** — pure location. **The campaign had the location mask in hand three times at sweep_B** — the 𝔖-correlation, the "mean drift", and the 3\|N mean split of §11 — and read all three as decaying residuals of a scale law. §12 rewritten |
| 44 | the mask should appear as an **observable excess** in the Goldbach count, since Theorem C gives `r̃(N) − 𝔖(N)N ≈ −𝔖(N)C(N)` | **it does not — E₃ absorbs it.** The relation is real (corr `+0.701`, slope `+1.263` against a predicted 1, permuted corr `+0.0001`), but resolved by depth the slope falls 2.15 → 1.67 → 1.40 → 0.91 → 0.43 → **0.18**: exactly where the mask is largest it is least visible. Since `R = P + E₃` by definition, a slope of 0.18 means `cov(E₃,P)/var(P) = −0.82`, i.e. **E₃ cancels 82% of the mask's contribution**. Consistent with `rms E₃/rms 𝔖C` growing 1.02 → 1.36 and `E₃ ∼ N^0.65` against the mask's `N^0.5`. The mask is a real second-order term of C(N) and **not** an observable bias in r̃ |
| 43 | the wall's location mask should have an analogue on the **supply side**, since `q\|k, q∤N` forces `N−mk` coprime to q | **refuted.** No group of k shows a mean shift (all \|z\| < 1.2; prime-by-prime max 1.65 against a permuted control reaching 1.66). The reason is structural: the wall's mask comes from **Λ** forcing v coprime to rad(N), a *primality* constraint, whereas both factors of D(k) are μ, which imposes squarefreeness. The analogue is therefore a **support** mask, not a location shift: `q²\|gcd(k,N)` ⟹ `q²\|N−mk` for every m ⟹ **D(k) = 0 identically**. All 1212 predicted zeros observed. En route, `P(D>0) = 0.346` looked like a z = −12.7 refutation of Conjecture L's Gaussian half; it was the atom at zero, and excluding zeros gives 0.5044, z = +0.46 |
| 42 | increment 248's residual after mask removal (z ≈ ±5) is a **missing piece of the mask** | it is **skewness**, and no mask is missing. Raising the enumeration from q ≤ 13 to q ≤ 37 moves z(3\|N) only 5.51 → 4.18 and leaves z(3∤N) flat at ≈ −5.2, so truncation is refuted. Subtracting a cell **mean** zeroes the mean, not the median, and for mean-zero data `P(X>0) − 1/2 = −γ₁/(6√(2π))` with no fitted constant: measured +0.00317 against +0.00258 for 3\|N (ratio 1.23) and −0.00233 against −0.00103 for 3∤N (2.27). **The enumerated mask is the whole of the deterministic part.** Recorded with it: the Edgeworth term was first written with a **plus**, which inverted every verdict |
| 41 | sweep_B item **B4**: the sign balance of C(N) reads `P(C>0) = 0.4800, z = −1.55`, i.e. **no signal** | a **false negative from underpowering**. B4 ran on ~1500 values, SE 1.3·10⁻², against an effect the mask makes structured rather than uniform. At 1.95·10⁶ values (SE 3.6·10⁻⁴): `3\|N` gives **0.44153, z = −94.3**; `3∤N` gives **0.52703, z = +61.6**; the gap is z = −112.6. By depth, N divisible by five of {3..23} has `P(C>0) = 0.109` — **89% negative**. Removing the enumerated mask returns all of it to within z ≈ 5 of 1/2 |
| 40 | the derived mask `R_A(N) = Π_{q∤N}(q−3)/(q−1)`, from `P(q\|v) = 1/(q−1)` for `q∤N` with independent indicators | **refuted quantitatively, right qualitatively.** Its sharpest prediction — the factor at q = 3 is exactly 0, so `3∤N` kills the mask — fails: the mask does not vanish but **changes sign** (mean R_A = −6.10·10⁻⁴ for 3\|N against **+2.45·10⁻⁴** for 3∤N, ratio 2.49 against a criterion of 5). Per-prime factors measure 12.98, 4.70, 2.91, 2.62, 2.28, 2.16, 2.02 against a predicted 2.00, 1.50, 1.25, 1.20, 1.143, 1.125, 1.10 — decreasing as predicted but toward **2, not 1**, which no per-prime effect can do. One fitted constant gives R² = +0.381 and under-predicts the deep cells 3–4×. The independence of the divisibility indicators, given that N−v is prime, is the identified culprit |
| 39 | increment 243's position: the location mask has **no closed form**, only an enumerated table | it has a **derivation**. n prime and q\|N force q∤(N−n), so v is confined to the integers coprime to rad(N), where the primes (μ = −1, no sign variation) are over-represented and the Möbius sum carries a bias. With `κ(N) = Π_{q\|N} q/(q−1)` **derived** — numerator ψ(N) ~ N, denominator N·Π(1−1/q) — the prediction `C(N) ≈ κ₀·κ(N)·M_{rad N}(N)` has **one** free constant and reaches weighted R² **+0.569** on the cell means, against +0.219 for a fitted nine-parameter additive model. Not complete: it under-predicts the deepest cells by ~1.7× |
| 38 | Conjecture L's mask applies to the **scale** of a μ-family | it applies to the **location** too, and there it is the larger effect. The mean of `C(N)/√(V(N))` is a deterministic function of *which* small primes divide N — not of 𝔖(N), and not additively: an additive one-coefficient-per-prime model predicts −0.49 for N = 510510 where the truth is −8.99, a factor 18. Full modular enumeration over q ∈ {3,…,23} takes the excess kurtosis from +0.468 to **+0.014**, every tail inside 3 SE, and the extreme from z = +16.8 to **+0.61** |
| 37 | increment 237's reading: the wall's fluctuation has a **heavy tail**, refuting Conjecture L's Gaussian half | a **location** effect, not a tail one. The outliers are primorials — 510510 = 2·3·5·7·11·13·17 and its relatives — and **all twelve have C < 0**. When n is prime, N−n is forced coprime to rad(N), so v is drawn from integers with no small prime factor, ω(v) shifts down, and session 8's alternating balance tilts toward ω = 1 where μ = −1. Centring on an 𝔖(N)-dependent mean removes **96%** of the excess kurtosis (+0.468 → +0.019) and the extreme from z = +16.8 to **+2.4, which passes**. Conjecture L needs a **mean** term — a local mask on the *location*, not only on the scale |
| 36 | the wall's law `C(N) = √(𝔖(N)·N)·G(N)` with `G ∼ N(0,1)` | **`Var C(N) ≈ 0.465·𝔖(N)·N·log N`** — the recorded law is missing a factor `√(log N)`. Measured over every even N ≤ 4·10⁶: sd(G) drifts 2.069 → 2.306, and `sd²/log N` is flat to **0.5%** (fitted exponent 0.895; α = 0 gives CV 7.5%, α = 1 gives 1.0%). The normaliser that needs no fitting is the exact second moment `Σ_v μ²(v)Λ(N−v)²`, against which the measured variance is 1.01 → 0.87. **Scale-normalisation drift, third instance** |

Two of these are the same species — #30 and REVIEW_VERDICT #3 (SEAM's
√P over-normalization) are both **scale-normalisation drift**: writing
a target at the square-root scale when the chain consumes it at the
trivial scale. Both were caught only by comparing a stated target
against our own measurements of the same quantity. That comparison is
now mandatory before any target is used to adjudicate anything.

## Tally after both rounds

| | count |
|---|---|
| void | 2 (C-III #1, C-III #4 arithmetic) |
| downgraded, not fatal | 2 (RV #6, C-III #4 structural) |
| moot (immaterial even if void) | 2 (RV #4, RV #5) |
| standing | the rest |

The negative map is smaller than it was, and everything that shrank
had been measured with the wrong ruler. What survives is structural:
violated premises, absent congruences, illegitimate transforms, and
kill-tests that found *no* signal rather than *insufficient* signal.

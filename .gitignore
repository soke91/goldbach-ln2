# The program's own record: process documents, exploratory code and the
# results that the paper does not cite. Kept on disk, deliberately not
# distributed -- it is a lab notebook, not a deliverable.
v1_log/
v1_verify_log/
v1_verify2_log/
v2_log/

__pycache__/
*.pyc

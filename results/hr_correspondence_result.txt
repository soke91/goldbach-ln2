(1) ladder identity  mu(m)mu(N-mk) = -mu(m/p)mu(N-(m/p)(pk))
    Note first that N - (m/p)(pk) = N - mk exactly: the move
    leaves the SECOND factor untouched and only refactorises
    u = mk. So the identity is really mu(m) = -mu(m/p),
    which holds precisely when p^2 does not divide m. The
    test enforces that hypothesis; a run that does not
    enforce it fails on ~10% of draws, which is exactly the
    density of p^2 | m, p^3 not | m with both mu factors
    nonzero -- a test fault, not a failure of the identity.
    tested 83592 (skipped 31866 with p^2 | m), mismatches 0   EXACT

(2) the move preserves u = mk, so the ladder's graph is
    the divisor lattice of u; summing the whole lattice is
    the complete divisor sum Sum_{m|u} mu(m) = [u=1]
           u  #divisors  sum mu(m)   [u=1]
           1          1          1       1
          30          8          0       0
         210         16          0       0
        2310         32          0       0
       30030         64          0       0
      510510        128          0       0
     1234567          4          0       0
    CONSISTENT

Reading: the ladder is exact but its graph is the divisor
lattice, whose complete sum is the identity we already have.
HR's engine is a move that is ADDITIVE (n -> n +- p) while
INDEXED multiplicatively (p | n). Ours is multiplicative in
both coordinates, so no new information is available from it.
DONE

(1) control -- lambda(pn)lambda(pn+p) = lambda(n)lambda(n+1)
    tested 200000, mismatches 0   HOLDS -- the mechanism

(2) our wall -- is mu(pv)Lambda(N-pv) tied to mu(v)Lambda(N'-v) for any N'?
    Lambda is not completely multiplicative: Lambda(pm) is
    log p when m is a power of p and 0 otherwise, so no
    factor of the form Lambda(p) x Lambda(m) exists. The
    check below reports how often Lambda(p*m) equals
    anything proportional to Lambda(m).
    Lambda(m) nonzero in 23124 draws; Lambda(pm) also nonzero in 74 (0.0032)
    => the second factor does not survive dilation: NO simultaneous dilation

(3) mu-mu -- mu(pv)mu(pN'-pv) = mu(v)mu(N'-v), p not| v
    tested 133173, mismatches 0   HOLDS
    but note what it relates: the correlation at N = p N'
    restricted to p | v, against the correlation at N'.
    The dilation moves N, so it acts ACROSS the N-family,
    not within a single N -- which is the N-average again.

Conclusion: the Tao/HR mechanism needs both factors to
transform under the SAME dilation. Our wall pairs mu with
Lambda and only mu does. Replacing Lambda by mu restores the
dilation but moves N, landing back on the N-average.
DONE
